# 后台管理端验收测试用例清单 · 代码审计报告

- **审计时间**：2026-08-29
- **代码基线**：HEAD `8d99a20`
- **审计方式**：逐条比对清单与 `client/src/pages`、`client/src/components` 及 `server/src/routes`（reservations / orders / shares / galleries / customerMine / mobileMessage）与 `server/src/todo.js`
- **总体结论**：**60 条用例中，通过约 26 条，部分通过约 9 条，未通过约 25 条**；黑名单 6 项中命中 3 项、部分命中 2 项

图例：✅ 通过 ｜ ⚠️ 部分通过 ｜ ❌ 未通过

---

## 一、预约模块（8 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 1.1 | 列表完整展示新郎/新娘/双手机号/地点/套系/备注 | ✅ | `Reservations.jsx` 表格字段齐全 |
| 1.2 | 详情可执行 待确认/已沟通/已拒绝/转订单 | ✅ | `reservations.js:60-75` PATCH status 支持 pending/contacted/rejected；converted 走 `/:id/convert` |
| 1.3 | 转订单弹窗预填；**执行人、套系、定金必填**才可生成 | ⚠️ | 执行人 ✅（`:119`）、定金 ✅（`:120-122`）、**套系未校验**（`:124` 注释「套系（可为空）」） |
| 1.4 | 生成新订单；预约转「已转订单」；order_id 绑定 | ✅ | `reservations.js:143-160` INSERT orders + `UPDATE reservations SET status='converted', order_id=?` |
| 1.5 | C‑H5 同步出现订单 | ✅ | `customerMine.js` my-business 实时按手机号查库 |
| 1.6 | **拒绝预约可填备注；C 端可见备注** | ❌ | PATCH `/:id/status`（`:60-75`）**只接 `status`，不接 remark**；`Reservations.jsx:201` 拒绝按钮直接 `changeStatus(id,'rejected')`，**无备注输入**。C 端自然也拿不到拒绝原因 |
| 1.7 | 新预约通知 + 消息 Tab 角标 | ✅ | `customerMine.js:175-180` `emitBizToStaff({sub_type:'reserve'})`；`Topbar.jsx:78-80` / `MobileShell TabBar` 轮询 `/api/mobile/message/unread-count` |
| 1.8 | 并发提交不重复生成单据 | ✅ **优秀** | `customerMine.js:138-158` 手机号 60s 乐观占位 + 10 分钟 3 次 + IP 兜底；写入失败自动释放占位 |

## 二、订单模块（6 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 2.1 | 新建订单支持双手机号，**两个号都能在 C‑H5 登录** | ❌ **P0** | `OrderCreateModal.jsx:272` 只取 `phones[0]` 写入 `customer_phone`；后端 `orders.js` **从不写 `phone_two`**（全路由 grep 无写入）。第二手机号落在 `phones` JSON 与 `bride_phone`，而 C 端登录查 `customer_phone = ? OR phone_two = ?` → **第二手机号登录不了** |
| 2.2 | 改绑手机号 C 端权限实时生效；解绑即失效 | ⚠️ | `customer_phone` 可改且 C 端实时查库 ✅；但 **`phone_two` 在 `orders.js` 的 PUT 中完全未处理** → 第二手机号无法改绑/解绑 |
| 2.3 | 客户相册**禁止 share‑key**，仅手机号鉴权 | ❌ **P0** | 同 C 端审计：`share.js:251-258` `buildPayload` 支持 `case 'order'`，`legacyOrderShare()` 走 `orders.share_token` → 客户底片可凭随机密钥直开 |
| 2.4 | 上传相册图片走 CDN；C 端懒加载展示 | ✅ | COS/R2 存储 + `img()` 只传 URL；`AlbumGrid` 有 `loading="lazy"` |
| 2.5 | 改状态/档期，C‑H5 刷新同步 | ✅ | C 端每次进页面实时查库 |
| 2.6 | 归档后 C 端不展示；后台归档列表可翻看 | ✅ | `orders.js:243` `WHERE is_deleted = 1` 归档列表；`customerMine.js` 查询带 `is_deleted = 0` |

## 三、公开样片合集 share‑key（7 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 3.1 | 新建合集；增删作品；作品多合集复用 | ✅ | `galleries.js` CRUD 完整 |
| 3.2 | 开启分享生成 share‑key；链接自动带微信提示备注 | ⚠️ | 生成 key ✅（`shares.js:55` POST）；但备注默认值 `getConfig('customer_order_share_default_note','')` **默认为空串**，后台未配置则不带备注 |
| 3.3 | 关闭分享 `is_share_open=false` → 「该合集已关闭对外分享」 | ❌ | **无 `is_share_open` 字段**（全库 grep 零命中）；实际用 `shares.disabled` + `toggle`（`shares.js:120-130`）；C 端文案 `该分享已被关闭` ≠ 要求文案 |
| 3.4 | **重置 share‑key**，旧链接失效并生成新链接 | ❌ | **`shares.js` 无重置接口**（只有 POST/GET/toggle/DELETE/logs），前端亦无「重置」入口 |
| 3.5 | 删除合集，旧 share‑key 打开提示失效 | ✅ | `galleries.js:106-109` 删除合集时同步清理 `share_logs` 与 `shares` |
| 3.6 | **复制分享链接自带备注文本** | ❌ | `OrderDetail.jsx:899-903` `copyShare()` 只 `writeText(share.share_url)`，**不拼接备注**；且用 `alert()` 弹出 |
| 3.7 | 访客埋点回传，后台可看访问记录 | ✅ | `shares.js:150` `GET /:token/logs` + `logAccess()` |

## 四、消息中心（5 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 4.1 | 新预约自动生成通知 + 未读角标 | ✅ | 同 1.7 |
| 4.2 | 点击跳详情 + 标记已读 + 角标减少 | ✅ | `MessageCenter.jsx:31-35` biz_type→路径；`:71` 详情接口后端自动标记已读；`biz_exist` 校验 |
| 4.3 | 区分已读/未读；支持全部标为已读 | ✅ | 已读 `opacity: 0.75`；`ReserveMessages`/`OrderMessages`/`MobileMessageList` 均有「批量标记已读 / 全部标为已读」 |
| 4.4 | 消息不无限堆积；可翻看历史 | ⚠️ | 有分页与手动删除（`mobileMessage.js:138/221`），但**无保留周期/自动清理策略** |
| 4.5 | 无悬浮弹窗，只在消息 Tab 内处理 | ✅ | 仅 badge 角标 + 一次性 `tip`，无常驻浮窗 |

## 五、自媒体工作台 & 今日待办（8 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 5.1 | 左侧「自媒体」入口，可进工作台 | ✅ | 侧边栏已注册 `/media` 及 7 个子路由 |
| 5.2 | 灵感库录入 + 自定义标签 + 抖音/小红书链接提取 | ✅ | `MediaInspirations.jsx` 完整实现 |
| 5.3 | 看板拖拽换列 + 表格视图切换 + 页面内编辑 | ✅ | `MediaBoard.jsx` 双视图 + 拖拽 |
| 5.4 | 卡片自定义颜色；违禁词仅提示不拦截 | ✅ | `COLOR_PRESETS`；`checkBanned` 命中只 alert 不阻断保存 |
| 5.5 | AI 复盘按钮生成报告并入库 | ✅ | `MediaReview.jsx` `runSkill('review_report')` + `POST /api/media/reviews` |
| 5.6 | **今日待办子模块**：快捷输入 / 编辑弹窗 / 颜色 / 本周计划 | ❌ | `client/src/pages/media/` **下无任何待办文件**（只有 8 个 Media*.jsx + 3 个 js）。现有 `Todo.jsx` 是业务待办（订单/预约驱动），**无快捷输入框、无编辑弹窗、无待办颜色标记、无「本周计划事项」视图** |
| 5.7 | 筛选 全部/已完成/未完成；优先级；归档可翻看 | ❌ | `Todo.jsx` 仅按 `todo_type` 分 Tab 且只显示 `pending`；**无 全部/已完成/未完成 切换、无优先级、无归档列表** |
| 5.8 | **一个选题最多对应 1 条待办** | ❌ | `server/src/todo.js` **无任何 `topic` / `media_topic` 引用**，选题与待办完全无关联 |

## 六、权限、表单、操作兜底（5 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 6.1 | 提交按钮 loading 置灰防重复 | ⚠️ | `Works.jsx` 8 处 ✅；`Orders.jsx` 2 处、`Packages.jsx` 2 处偏少；**`Reservations.jsx` 0 处**——「确认生成订单」按钮（`:291`）无 `disabled`/loading，可连点生成多张订单 |
| 6.2 | 必填项就近提示，不弹全局弹窗 | ❌ | 大量 `alert()` 全局弹窗：`Reservations.jsx` 5 处、`Orders.jsx` 5 处、**`OrderDetail.jsx` 47 处**、`Works.jsx` 7 处、`Packages.jsx` 4 处（如 `Reservations.jsx:84` `alert('请填写定金金额')`） |
| 6.3 | 5xx 友好提示，**禁止暴露堆栈** | ❌ **P0 系统性** | 后端 **302 处** `res.status(500).json({ error: e.message })`（37 个路由文件）；前端 `api.js:75` 直接 `toast(data.error \|\| '请求失败('+status+')')` |
| 6.4 | 删除类操作二次确认 | ⚠️ | `OrderDetail.jsx` 8 处、`Orders.jsx` 1 处、`Works.jsx` 1 处；**`Reservations.jsx` 0 处** |
| 6.5 | 空状态友好 UI（预约/订单/消息/待办） | ✅ | `Reservations` / `Orders`(3) / `MessageCenter` / `Todo` 均有空态 |

## 七、前后端联动（5 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 7.1 | 改预约状态 → C‑H5 同步 | ✅ | C 端实时查库 |
| 7.2 | 预约转订单 → C‑H5 出现订单 | ✅ | 同上 |
| 7.3 | 关闭/重置 share‑key → C‑H5 对应提示 | ⚠️ | 关闭 ✅（`disabled`→403 `该分享已被关闭`）；**重置 ❌** 无此能力 |
| 7.4 | 改订单绑定手机号 → C‑H5 权限同步 | ⚠️ | `customer_phone` ✅ 实时生效；**`phone_two` ❌** 后台无法编辑 |
| 7.5 | 上传/删除相册图片 → C‑H5 渲染或占位图 | ⚠️ | 渲染 ✅；失效占位图 ❌（`AlbumGrid` 仅 `opacity=0.3`，单列大图无 `onError`） |

---

## 八、BUG 黑名单命中情况

| # | 禁止现象 | 命中 | 说明 |
|---|---|---|---|
| 1 | 转订单不校验必填直接生成残缺订单 | ⚠️ **部分命中** | 执行人/定金已校验；**套系未校验**可为空 |
| 2 | 客户相册产生 share‑key 可访问底片 | ❌ **命中** | `buildPayload('order')` + `legacyOrderShare` |
| 3 | 后台操作完成 C‑H5 无同步 | ⚠️ **部分命中** | 大部分实时同步；`phone_two` 改绑不同步 |
| 4 | 重复提交生成多条重复单据 | ✅ 未命中 | 预约侧手机号 60s 去重完善；**但转订单按钮无 loading**（见 6.1，属同类风险） |
| 5 | 后端报错码/堆栈渲染到后台 | ❌ **命中** | 302 处 `e.message` 直出 |
| 6 | 关闭分享与重置 share‑key 逻辑混淆 | ❌ **命中** | 只有 toggle/删除，**无独立的重置密钥能力**，两个语义被混为一谈 |

---

## 与 C 端审计的交叉结论

以下 3 项是**两端共用的同一处后端缺陷**，修一次可同时解掉 C 端与后台的黑名单项：

1. `share.js` 的 `order` 分支与 `legacyOrderShare()` —— 解 C 端 1.9/4.2/黑名单3 + 后台 2.3/黑名单2
2. 后端 302 处 `e.message` 直出 —— 解 C 端 5.3/9.2/黑名单1 + 后台 6.3/黑名单5
3. `orders.phone_two` 无写入路径 —— 解 C 端 1.2 第二手机号登录 + 后台 2.1/2.2/7.4

## 建议修复顺序（待确认）

**P0**
1. `orders` 双手机号：建单/编辑均落 `phone_two`，保证两个号都能 C 端登录
2. 禁止 share-key 访问客户专属相册
3. 后端统一错误出口 `serverError()`：只回「服务暂时繁忙，请稍后再尝试」，堆栈仅 `console.error`
4. 转订单补齐「套系」必填 + 「确认生成订单」按钮 loading 置灰
5. 拒绝预约支持填写备注并回传 C 端
6. 分享「重置密钥」独立接口（与「关闭分享」彻底分开）

**P1**
7. 新增 `is_share_open` 语义（或统一为 `disabled`）并对齐 C 端三套文案
8. 复制分享链接拼接备注文本
9. 后台 `alert()` 改字段级就近提示（优先 `OrderDetail.jsx` 47 处）
10. 预约删除二次确认
11. 消息保留周期策略

**P2 · 需产品确认（工作量最大）**
12. 自媒体下新建「今日待办」子模块（快捷输入/编辑弹窗/颜色/本周计划/筛选/优先级/归档），并建立「选题 ↔ 待办 1:1」约束
