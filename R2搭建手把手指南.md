# Cloudflare R2 图片存储 · 手把手搭建指南

> 适用：把摄影工作室系统的图片（作品 / 客户相册 / 选片小样）从「本地 uploads（Render 重启会丢）」迁移到 **Cloudflare R2 私有桶 + Worker 代理**。
> 原则：前端永不直连 R2 密钥，上传由后端用 S3 协议写私有桶，读取走 Worker 代理域名。R2 有免费额度（10GB 存储 + 每月 1000 万次 A 类 / 1000 万次 B 类请求），正常使用**不花钱**。

---

## 0. 现状说明（先看懂再动手）

系统里 `server/src/storage.js` 已经写好适配逻辑：

- **未配 R2** → 图片存本地 `server/data/uploads`，返回 `/uploads/xxx`（Render 免费档重启会清空，需重传）。
- **五项环境变量齐全** → 图片传到私有 R2 桶，返回 `https://<你的Worker域名>/r2/<zone>/<文件名>`。

所以你**不需要改任何代码**，只需要在 Cloudflare 控制台开通 + 填 5 个环境变量即可。下面一步一步来。

环境变量清单（最终要填到 Render 的 Environment 或本地 `.env`）：

| 变量名 | 含义 | 例 |
|---|---|---|
| `R2_ENDPOINT` | R2 的 S3 兼容端点 | `https://<accountId>.r2.cloudflarestorage.com` |
| `R2_BUCKET` | 桶名 | `yezhe-studio` |
| `R2_WORKER_DOMAIN` | 读取用的 Worker 代理域名（带 https，尾部不要斜杠） | `https://yezhe-r2-proxy.your-sub.workers.dev` |
| `R2_ACCESS_KEY` | R2 API Token 的 Access Key ID | 一长串 |
| `R2_SECRET_KEY` | R2 API Token 的 Secret Access Key | 一长串 |

---

## 1. 注册 / 登录 Cloudflare（免费）

1. 打开 https://dash.cloudflare.com/sign-up 注册账号（邮箱即可）。
2. 登录后进入主面板。**R2 需要绑定一种付款方式才能创建 API Token**（Cloudflare 要求，但免费额度内不会扣费）。按提示添加信用卡/借记卡，确认「Free 套餐」即可。
3. 记下右侧的 **Account ID**（后面 `R2_ENDPOINT` 要用）。

> 如果你已有 Cloudflare 账号（比如已用它的 DNS），直接复用即可。

---

## 2. 创建 R2 桶

1. 左侧菜单进入 **R2 Object Storage**（或访问 https://dash.cloudflare.com/?to=/:account/r2）。
2. 点 **Create bucket**。
3. 桶名填 `yezhe-studio`（全局唯一，随便起但记得住就行），区域选 **Automatic**。
4. 点创建。**免费额度 10GB，足够工作室用。**

---

## 3. 创建 R2 API Token（写权限）

1. 在 R2 页面右上角点 **Manage API Tokens**（或 **API Tokens**）。
2. 点 **Create API Token**。
3. 权限选：
   - **Permission**: `Object Read & Write`（读写）
   - **Specify bucket**: 选 `yezhe-studio`（只授权这一个桶）
   - **TTL**: 选 `Permanent`（永久，省得以后失效；也可设 1 年）
4. 点 **Create**。
5. **关键**：页面会显示 **Access Key ID** 和 **Secret Access Key**。
   - `Access Key ID` → 后面填 `R2_ACCESS_KEY`
   - `Secret Access Key` → 后面填 `R2_SECRET_KEY`
   - ⚠️ **Secret 只显示一次**，立刻复制保存好。

同时回到桶页面复制 **Account ID**（步骤 1.3），拼出：
`R2_ENDPOINT = https://<Account ID>.r2.cloudflarestorage.com`

---

## 4. 创建 Cloudflare Worker 做读取代理

后端写图用 S3 协议（带密钥，安全）；但**前端读图不能带密钥**，所以用一个 Worker 从 R2 私有桶取图再转发。这样你的桶永远是私有的。

1. 左侧菜单 **Workers & Pages** → **Create** → 选 **Worker** → 取名 `yezhe-r2-proxy` → **Deploy**。
2. 进入该 Worker，点 **Edit code**，把默认代码**全部替换**为下方代码：

```js
// yezhe-r2-proxy —— 私有 R2 读取代理（仅放行 /r2/* 路径）
export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (url.pathname.startsWith('/r2/')) {
      const key = url.pathname.slice(4); // 去掉前缀 "/r2/"
      if (!key || key.includes('..')) return new Response('Bad key', { status: 400 });
      const object = await env.BUCKET.get(key);
      if (!object) return new Response('Not found', { status: 404 });
      const headers = new Headers();
      object.writeHttpMetadata(headers);
      headers.set('Cache-Control', 'public, max-age=31536000, immutable');
      headers.set('Access-Control-Allow-Origin', '*'); // 允许小程序/web 跨域读图
      return new Response(object.body, { headers });
    }
    return new Response('yezhe r2 proxy ok', { status: 200 });
  }
};
```

3. 切到 **Settings → Variables → R2 Bucket Bindings**，点 **Add binding**：
   - **Variable name** 填 `BUCKET`（必须叫这个，代码里用的是 `env.BUCKET`）
   - **R2 Bucket** 选 `yezhe-studio`
   - 保存。
4. 回到 **Deployments** 标签，点 **Redeploy**（让 bindings 生效）。
5. 部署完成后，Worker 会有一个地址形如 `https://yezhe-r2-proxy.<你的sub>.workers.dev`。
   - 记下它 → `R2_WORKER_DOMAIN = https://yezhe-r2-proxy.<你的sub>.workers.dev`
   - 可浏览器访问一下这个地址，看到 `yezhe r2 proxy ok` 即正常。

---

## 5. 填环境变量（本地 + Render）

### 5.1 本地开发（`.env`）
在 `摄影工作室管理系统/server/.env` 追加：
```env
R2_ENDPOINT=https://<AccountID>.r2.cloudflarestorage.com
R2_BUCKET=yezhe-studio
R2_WORKER_DOMAIN=https://yezhe-r2-proxy.<你的sub>.workers.dev
R2_ACCESS_KEY=<你的AccessKeyID>
R2_SECRET_KEY=<你的SecretAccessKey>
```
（`server/.env.example` 里已有占位模板，照着填。）

### 5.2 线上 Render
1. 打开 Render 项目 `yezhe-studio-server` → **Environment**。
2. 逐个添加上面 5 个变量（名字完全一致，值不带引号）。
3. 保存后会自动重新部署；没有 Shell 也能生效。

---

## 6. 验证是否走通 R2

1. 重启 / 重新部署后端。
2. 打开系统后台 → 任意上传一张作品图。
3. 看返回的图片地址：
   - 如果是 `https://yezhe-r2-proxy.../r2/biz-works/...` → ✅ 已走 R2。
   - 如果是 `/uploads/...` → ❌ 环境变量没齐，后端仍落本地（检查 5 项是否都填、有无多余空格）。
4. 浏览器直接打开那个 `https://.../r2/...` 地址，能显示图片即代理正常。

也可临时在后端加一行日志看 `isR2Enabled()` 返回值，或在 `storage.js` 顶部打印 `r2Config()`。

---

## 7. 迁移旧图片（可选）

本地 `server/data/uploads/` 里已有图，想迁移到 R2：
- 简单做法：后台重新上传一次即可（最稳妥）。
- 进阶：写个小脚本用 S3 SDK 把 `uploads/` 下文件逐个 `PutObject` 到 `biz-works/` 等对应 zone，并把数据库里 `/uploads/xxx` 替换成 Worker URL。需要我帮你写这个迁移脚本可以再说。

---

## 8. 费用与安全提醒

- **免费额度**：存储 10GB/月、A 类请求 1000 万次/月、B 类请求 1000 万次/月免费。摄影工作室量级远低于此，**正常零费用**。
- **桶必须保持私有**：读写都靠后端密钥 + Worker，不要把桶设为 Public。
- **Secret Key 只显示一次**，泄露了就去 R2 → API Tokens 里 **Roll/Revoke** 旧的，重建一个。
- **跨域**：Worker 已加 `Access-Control-Allow-Origin: *`，小程序和网页都能读；如需更严格可改成你的域名。
- **Worker 域名**默认 `*.workers.dev` 在国内偶尔访问慢，正式上线可在 Cloudflare 给 Worker 绑一个自己的域名（需把域名 NS 接入 Cloudflare，免费）。

---

## 9. 故障排查

| 现象 | 原因 / 处理 |
|---|---|
| 上传报 403 / SignatureDoesNotMatch | Access/Secret Key 填错或多了空格；Account ID 与 Endpoint 不匹配 |
| 图片 404 | Worker 的 BUCKET 绑定名不是 `BUCKET`；或 key 路径不对（应 `/r2/<zone>/<file>`） |
| 仍走本地 `/uploads` | 5 个变量没齐；Render 改完要等重新部署完成 |
| 小程序看不到图 | Worker 没加 CORS 头；或 Worker 域名被墙，绑自己域名 |
| 冷启动慢 | 首次请求 Worker/R2 冷启动，正常，Render 免费档同理 |
