# 开发规范：禁止写死静态模拟数据，全部接口驱动

> 生效日期：2026-08-06 ｜ 适用范围：`client/`（Web H5 后台/客户前端）、`miniprogram/`（微信小程序 C 端）
> 后端：`server/`（Render + Neon Postgres / 本地 SQLite）。所有展示数据均来自后端实时接口，绝不前端写死。

---

## 一、核心原则

所有展示给用户的**数字、列表、分类、作品、订单状态、金额、预约记录、关注状态、客户相册**，
必须请求 Render 后端 API 接口、从数据库实时读取后渲染页面。

- ✅ 允许：后端 `server/src/seed.js` 在数据库为空时自动插入 **1 套初始化演示数据**（admin + 4 预设分类 + 样例子模块），用于预览。
- ❌ 禁止：`wxml / js / jsx / vue` 页面代码里直接写死业务数组、数字、分类名称、状态枚举、金额、静态记录。
- ❌ 禁止：接口无数据时 fallback 渲染一套写死的假数据；只允许展示**空状态提示文字**。

---

## 二、8 条规则（强制）

1. **页面业务数据全部接口化**：数字、列表、分类、作品、订单状态、金额、预约记录，严禁在前端/小程序写死硬编码模拟数据，全部请求 Render 后端 API 实时获取。
2. **仅后端初始化 1 套样例**：只有后端数据库初始化脚本（`seed.js`）可插入 1 套测试示例；正式运行后前端所有渲染来源必须来自接口，不得写死任何业务数据。
3. **C 端小程序首页作品 Tab 分类接口驱动**：
   - 小程序只调用后端分类接口拿到数组再循环渲染 Tab 栏；
   - 初始默认数据由后端库预置（婚礼/领证/写真/家庭纪实）；商家在 H5 后台分类管理可增删改禁用，前端/小程序跟随接口返回动态变化；
   - **后台改分类，小程序下拉刷新即生效，无需修改代码、无需重新发布版本。**
4. **H5 工作台看板统计接口化**：余额、待处理订单各类统计数字（未支付定金、等待拍摄、待选片、待精修、未交片等）全部调用后端统计接口（ `/api/stats` ）实时计算返回，页面渲染接口返回值，不写死固定数字。
5. **各列表分页接口化**：作品列表、套系列表、档期列表、订单列表，全部由后端接口返回分页数据，前端循环渲染，禁止页面内写死样例作品/样例订单。
6. **客户状态实时化**：关注状态、预约表单提交结果、客户个人中心预约/相册数据，全部读取接口实时状态，不写死布尔值与静态记录。
7. **区分「后端预置测试数据」与「前端硬编码静态数据」**：
   - ✅ 允许：后端 `seed.js` 插入 1 套测试示例数据；
   - ❌ 禁止：wxml、js、vue 页面代码里直接写死业务数组、数字、分类名称。
8. **异常兜底为空状态**：当接口无数据返回（数据库为空），页面展示空状态提示文字，不能 fallback 渲染一套写死的假数据。

---

## 三、允许的例外（非业务数据，可固定）

| 场景 | 说明 | 是否违规 |
| --- | --- | --- |
| UI 结构 / 导航入口 | 工作台卡片入口、侧边栏菜单、各页「管理」按钮等纯前端路由结构 | 不违规（不含业务数值） |
| 统计块中文标签与配色 | Dashboard 待办块 `未支付定金/等待拍摄…` 的**显示标签**与配色，为 UI 元数据；**数量**仍来自 `/api/stats` | 不违规 |
| 分类 Tab 的「全部」聚合标签 | 前端固定筛选元标签 `id=0`，不存数据库；业务分类由 `/api/categories` 返回循环渲染 | 不违规 |
| 空值占位 `‖ '—'` / `‖ []` | 仅用于接口为空时的空状态占位，不渲染假业务数据 | 不违规 |
| 帮助文案里的示例 JSON | 如套系问卷 ` [{"q":"婚礼日期"...}] ` 仅作输入框占位说明 | 不违规 |
| 品牌名称空状态占位 | 接口未返回 `studio.name` 时，用固定品牌名「叶哲 STUDIO」作为 UI 占位，不属于业务数据 | 不违规 |

> 判定红线：**凡是会随商家在后台增删改而变化的业务内容，或会随数据库记录数变化的数字，都不得写死。**

---

## 四、日志监督校验项（每个模块完成必须核对输出日志）

- ✅ 1、页面业务数字、列表、分类均来自后端接口，前端无硬编码业务数据
- ✅ 2、小程序 Tab 分类完全由后端接口驱动，代码内没有写死分类字符串
- ✅ 3、仅后端初始化脚本存在一套测试样例，前端无写死模拟业务数据
- ✅ 4、数据库为空时展示空状态，不会自动渲染静态假数据
- ✅ 5、修改后台分类配置，小程序下拉刷新即可生效，无需发布小程序版本
- ✅ 6、品牌名称/简介/口号/联系方式均来自 `/api/settings/studio`，前端无硬编码业务联系信息

---

## 五、当前代码合规审计证据（2026-08-06）

| 项 | 证据 |
| --- | --- |
| 分类接口化 | `miniprogram/pages/index/index.js` `loadCategories()` → `GET /api/categories`；`pages/works/works.js` `loadCats()` 同；`components/customNav/customNav.js` 同；`client/src/pages/Home.jsx:37`、 `client/src/pages/Works.jsx:63` 同 |
| 分类 Tab 渲染 | 仅 `全部` 为固定元标签（`id=0`），业务分类 `wx:for` / `.map` 循环接口返回数组 |
| 看板统计接口化 | `client/src/pages/Dashboard.jsx` 余额/线上收入/线下收入/待办数均来自 `GET /api/stats`；后端 `server/src/routes/stats.js` 实时 SQL 计算（按 `orders.status` 分组计数） |
| 列表分页 | 作品 `GET /api/works/public?page=&pageSize=&category=`；套系/档期/订单均走对应后端分页接口 |
| 客户中心实时 | `miniprogram/pkg/appointment`、`pkg/order`、`pkg/evaluate`、`pkg/subscribe` 均实时拉取；「关注」为复制微信号，无写死布尔 |
| 测试数据仅后端 | `server/src/seed.js` 仅当表为空时插入 1 套演示（admin + 4 分类 + 2 套系 + 1 档期 + 1 订单 + 1 作品） |
| 空状态 | 各页 `wx:elif` / 条件渲染「暂无作品」等，无假数据兜底 |

## 六、真机/体验版必配：小程序合法域名

开发者工具可勾选「不校验合法域名」绕过，但**真机、体验版、正式版会强制校验**。若未配置，小程序所有 `wx.request` / 图片下载都会失败，表现为首页空白、相册不显示、下拉刷新无数据。

请在 [微信公众平台](https://mp.weixin.qq.com) → 小程序后台 → **开发管理 → 开发设置 → 服务器域名** 中添加：

| 类型 | 必须添加的域名 |
| --- | --- |
| request 合法域名 | `https://yezhe-studio-server.onrender.com` |
| downloadFile 合法域名 | `https://yezhe-studio-server.onrender.com` |
| downloadFile 合法域名 | 腾讯云 COS 图片 CDN 域名（后端 `COS_CDN_DOMAIN`，如 `https://yezhe-xxx.cos.ap-guangzhou.myqcloud.com`） |
| downloadFile 合法域名 | `https://yezhe-img-proxy.yezhe128627.workers.dev`（R2 兜底，未启用 COS 时） |

> 若更换了 Render 后端域名、COS CDN 域名或 R2 Worker 代理域名，需同步替换上表并重新提交微信审核/发布。

> 结论：**现有代码已符合本规范，无需修改业务数据渲染逻辑。** 本文件为后续开发的强制约束。
