# 摄影工作室 B 端 + C 端低代码原型方案

> 目标：复刻拾光盒子 / Picbling 核心业务，剔除全部付费 VIP、AI 修图、相册印刷、付费分享档期，仅保留免费基础业务闭环。输出可直接用于低代码平台生成的结构化原型方案。

---

## 1. 项目定位与范围

### 1.1 双端定义
| 端 | 用户 | 作用 |
|---|---|---|
| B 端后台 | 摄影师 / 管理员 | 维护套系、客片、档期、订单、客户、数据，管理小程序展示内容 |
| C 端小程序 | 客户 | 浏览公开客片、预约、查看个人订单、在线选片、查看交付影集 |

### 1.2 必须剔除的付费/增值能力
- ❌ 开通 VIP / 会员付费
- ❌ AI 修图包 / AI 智能修图
- ❌ 相册印刷 / 冲印商城
- ❌ 付费分享档期 / 档期付费解锁
- ❌ 任何向客户收费的增值入口

### 1.3 全局硬性约束
1. B 端保存的数据，C 端实时同步刷新，**不允许以本地缓存作为数据源**。
2. 订单采用**软删除**（`is_deleted` 标记），存在回收站可恢复。
3. 多处支持 Excel 导出：订单导出、档期导出（CSV / Excel）。
4. 新建订单必须关联已有套系，自动带入价格与服务参数。
5. 订单选择拍摄日期后，自动占用档期日历对应日期。

---

## 2. 全局 UI 设计规范

### 2.1 B 端后台
| 项 | 规范 |
|---|---|
| 风格 | 简约浅色企业后台 |
| 主色 | `#409EFF`（Element-Plus 默认蓝） |
| 成功/警告/危险 | `#67C23A` / `#E6A23C` / `#F56C6C` |
| 圆角 | `8px` |
| 组件库 | Element-Plus |
| 布局 | 左侧固定导航 200–220px，顶部面包屑，内容区白色卡片 |
| 字体 | 系统字体栈，`PingFang SC`、`Microsoft YaHei`、`Helvetica Neue` |
| 间距 | 卡片内边距 20–24px，模块间距 16–20px |
| 表格 | `el-table`，操作列固定右侧，行高 56px |
| 按钮 | 主操作 `type="primary"`，危险操作 `type="danger"`，文字按钮用于次级操作 |
| 弹窗 | `el-dialog`，表单一列或两列布局，底部「取消 / 保存」 |

### 2.2 C 端小程序
| 项 | 规范 |
|---|---|
| 风格 | 简约干净摄影向 |
| 主色 | `#409EFF` |
| 背景 | 页面 `#F7F7F5`，卡片 `#FFFFFF` |
| 圆角 | 卡片 12px，按钮 8px，头像 50% |
| 导航 | 底部 tabBar 4 项：首页 / 作品 / 套餐 / 我的（或按本方案 首页 / 我的订单） |
| 图片 | 作品/影集大图全宽，瀑布流或单列大图 |

---

## 3. 左侧导航菜单（仅开发下面标注 P0 的模块，其余占位）

```
工作台（占位）
小程序（占位）
资料设置（占位）
套系 —— P0 必做
客片 —— P0 必做
档期 —— P0 必做
订单中心 —— P0 核心最高优先级
客户管理（占位）
容量管理（占位）
数据统计（占位）
```

占位菜单仅保留入口，点击后显示「功能开发中」提示页或简单数据面板，不实现复杂业务。

---

## 4. 数据表完整字段定义

### 4.1 通用字段约定
- 所有表均含：`id` 主键、`created_at`、`updated_at`。
- 金额统一使用 `DECIMAL(10,2)` 或浮点，禁止字符串存储。
- JSON 字段用于数组/对象：`JSON` 类型（低代码平台若无 JSON 可用 TEXT 存储）。
- 删除标记：`is_deleted`（0=正常，1=已删除软删）+ `deleted_at` 时间戳。

---

### 4.2 分类表 `categories`
用于套系分类与客片分组。

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键，自增 |
| name | VARCHAR(50) | 是 | 分类名称，如「婚礼跟拍」「领证跟拍」 |
| kind | VARCHAR(20) | 是 | 类型：`package` 套系分类 / `work` 客片分组 |
| sort | INT | 否 | 排序号，默认 0 |
| is_public | TINYINT | 否 | 是否对外展示，默认 1 |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

---

### 4.3 套系表 `packages`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| name | VARCHAR(100) | 是 | 套系名称 |
| category_id | INT | 否 | 关联分类表 |
| cover_url | VARCHAR(500) | 否 | 封面图 URL |
| price | DECIMAL(10,2) | 是 | 售价 |
| deposit | DECIMAL(10,2) | 是 | 定金 |
| retouch_count | INT | 否 | 精修张数 |
| raw_policy | VARCHAR(255) | 否 | 底片政策，如「300 张底片全送」 |
| duration | VARCHAR(50) | 否 | 拍摄时长，如「全天」 |
| description | TEXT | 否 | 服务说明 / 套系详情 |
| status | VARCHAR(20) | 是 | `on` 上架 / `off` 下架，默认 `on` |
| is_public_in_miniprogram | TINYINT | 否 | 是否在小程序展示，默认 1 |
| sort | INT | 否 | 排序号，默认 0 |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

**说明**：低代码生成表单时，套系编辑拆为 4 个 Tab（参考截图）：套系名称、价格及问卷、服务及加片、其他详情。免费版保留「问卷/验证手机号」字段占位，但不调用付费短信服务。

---

### 4.4 客片作品表 `works`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| title | VARCHAR(100) | 是 | 作品标题 |
| category_id | INT | 否 | 分组分类 |
| cover_url | VARCHAR(500) | 否 | 封面图 URL |
| description | TEXT | 否 | 作品说明 |
| is_public | TINYINT | 否 | 是否对外展示，默认 1 |
| sort | INT | 否 | 排序号，默认 0 |
| order_id | INT | 否 | 关联订单（可选，客片交付场景） |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

### 4.5 客片图片表 `work_photos`（1 作品 : N 图片）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| work_id | INT | 是 | 关联作品 |
| photo_url | VARCHAR(500) | 是 | 图片 URL |
| sort | INT | 否 | 排序号 |
| is_cover | TINYINT | 否 | 是否封面，默认 0 |
| created_at | DATETIME | 是 | 创建时间 |

---

### 4.6 档期表 `schedules`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| date | DATE | 是 | 公历日期，如 `2026-08-10` |
| lunar_date | VARCHAR(20) | 否 | 农历日期，如「六月廿三」 |
| period | VARCHAR(20) | 否 | 时段：`full` 全天 / `am` 上午 / `pm` 下午，默认 `full` |
| status | VARCHAR(20) | 是 | `free` 空闲 / `unpaid` 未付定金 / `shoot` 等待拍摄 / `closed` 档期关闭 |
| is_closed | TINYINT | 否 | 是否档期关闭，默认 0 |
| order_id | INT | 否 | 绑定订单 ID |
| order_no | VARCHAR(50) | 否 | 绑定订单编号（冗余，方便展示） |
| photographer | VARCHAR(50) | 否 | 摄影师/执行人 |
| note | VARCHAR(255) | 否 | 备注 |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

**颜色标记规则**：
- `closed`：灰色斜纹或置灰，不可预约。
- `unpaid`：黄色，表示已占档但客户未付定金。
- `shoot`：绿色，表示已付定金等待拍摄。
- `free`：白色/默认色。

---

### 4.7 订单主表 `orders`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| order_no | VARCHAR(50) | 是 | 订单编号，唯一 |
| customer_name | VARCHAR(50) | 是 | 客户姓名 |
| customer_phone | VARCHAR(20) | 否 | 客户联系电话 |
| package_id | INT | 是 | 关联套系 ID |
| package_snapshot | JSON | 是 | 套系快照（名称、价格、精修、底片等），防止套系修改后历史订单数据变化 |
| shoot_date | DATE | 否 | 拍摄日期 |
| shoot_period | VARCHAR(20) | 否 | 拍摄时段：`full`/`am`/`pm` |
| is_date_tbd | TINYINT | 否 | 是否日期待定，默认 0 |
| package_price | DECIMAL(10,2) | 是 | 套系价格（从快照复制） |
| deposit | DECIMAL(10,2) | 是 | 定金金额 |
| balance | DECIMAL(10,2) | 是 | 尾款金额 |
| paid_amount | DECIMAL(10,2) | 否 | 已收金额，默认 0 |
| refund_amount | DECIMAL(10,2) | 否 | 退款金额，默认 0 |
| payment_status | VARCHAR(20) | 是 | `unpaid` 未付 / `deposit` 已付定金 / `paid` 已付清 / `refunded` 已退款 |
| extras_total | DECIMAL(10,2) | 否 | 其他消费合计，默认 0 |
| shoot_location | VARCHAR(255) | 否 | 拍摄地点 |
| remark | TEXT | 否 | 备注 |
| channel | VARCHAR(50) | 否 | 渠道来源，如「小红书」「老客户推荐」 |
| executor_id | INT | 否 | 执行人 ID |
| executor_name | VARCHAR(50) | 否 | 执行人姓名（冗余） |
| status | VARCHAR(30) | 是 | 订单状态：见下方生命周期 |
| paid_at | DATETIME | 否 | 支付订单时间（付定金时间） |
| shot_at | DATETIME | 否 | 完成拍摄时间 |
| uploaded_at | DATETIME | 否 | 上传底片/精修时间 |
| completed_at | DATETIME | 否 | 订单完成时间 |
| raw_storage_days | INT | 否 | 原片保存期限（天） |
| retouch_storage_days | INT | 否 | 精修片保存期限（天） |
| raw_expire_at | DATETIME | 否 | 原片过期时间 |
| retouch_expire_at | DATETIME | 否 | 精修片过期时间 |
| share_token | VARCHAR(100) | 否 | 订单二维码分享 token |
| qr_url | VARCHAR(500) | 否 | 二维码图片 URL |
| is_deleted | TINYINT | 否 | 软删标记，默认 0 |
| deleted_at | DATETIME | 否 | 删除时间 |
| openid | VARCHAR(100) | 否 | 绑定客户微信 openid |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

### 4.8 订单状态生命周期
```
unpaid（未付定金）
  ↓ 收到定金
deposit（已付定金）
  ↓ 完成拍摄
shot（已拍摄）
  ↓ 客户提交选片
selecting（选片中）
  ↓ 选片完成，开始精修
retouching（精修中）
  ↓ 精修完成，交付客户
delivered（已交付）
  ↓ 客户确认 / 超时自动完成
completed（已完成）
cancelled（已作废）
```

### 4.9 订单其他消费子表 `order_extras`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| order_id | INT | 是 | 关联订单 |
| name | VARCHAR(100) | 是 | 消费项目名，如「加片 10 张」 |
| price | DECIMAL(10,2) | 是 | 单价 |
| quantity | INT | 是 | 数量 |
| total | DECIMAL(10,2) | 是 | 小计 |
| created_at | DATETIME | 是 | 创建时间 |

---

### 4.10 收款流水表 `payments`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| order_id | INT | 是 | 关联订单 |
| order_no | VARCHAR(50) | 是 | 订单编号冗余 |
| type | VARCHAR(20) | 是 | `deposit` 定金 / `balance` 尾款 / `extra` 其他 / `refund` 退款 |
| amount | DECIMAL(10,2) | 是 | 金额 |
| method | VARCHAR(20) | 否 | `offline` 线下 / `online` 线上 |
| note | VARCHAR(255) | 否 | 备注 |
| created_at | DATETIME | 是 | 创建时间 |

---

### 4.11 在线选片表 `photo_select`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| order_id | INT | 是 | 关联订单 |
| openid | VARCHAR(100) | 是 | 客户 openid |
| marks | JSON | 否 | 客户选中的照片 ID 数组 |
| draft | JSON | 否 | 草稿状态 |
| submitted | TINYINT | 否 | 是否已提交，默认 0 |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

---

### 4.12 评价表 `evaluates`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| order_id | INT | 是 | 关联订单 |
| openid | VARCHAR(100) | 是 | 客户 openid |
| stars | INT | 是 | 星级 1–5，默认 5 |
| text | TEXT | 否 | 评价文字 |
| images | JSON | 否 | 评价图片数组 |
| status | VARCHAR(20) | 是 | `pending` 待审核 / `approved` 通过 / `rejected` 驳回 |
| created_at | DATETIME | 是 | 创建时间 |

---

### 4.13 客户表 `customers`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| openid | VARCHAR(100) | 是 | 微信 openid，唯一 |
| nickname | VARCHAR(100) | 否 | 微信昵称 |
| avatar | VARCHAR(500) | 否 | 头像 URL |
| phone | VARCHAR(20) | 否 | 手机号 |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

---

### 4.14 预约表 `appointments`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| openid | VARCHAR(100) | 是 | 客户 openid |
| name | VARCHAR(50) | 是 | 称呼 |
| phone | VARCHAR(20) | 是 | 电话 |
| package_id | INT | 否 | 意向套系 |
| hope_date | DATE | 否 | 期望日期 |
| remark | TEXT | 否 | 备注 |
| status | VARCHAR(20) | 是 | `pending` 待跟进 / `converted` 已转单 |
| created_at | DATETIME | 是 | 创建时间 |

---

### 4.15 系统设置表 `settings`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| key | VARCHAR(50) | 是 | 主键，如 `studio` |
| value | JSON / TEXT | 否 | 配置内容 |

`studio` 配置示例内容：
```json
{
  "name": "叶哲 STUDIO",
  "logo": "",
  "cover": "",
  "intro": "海口婚礼 / 人像 / 纪实摄影",
  "contact": { "phone": "", "wechat": "", "address": "" },
  "tabs": [
    { "id": "home", "label": "首页", "visible": true },
    { "id": "works", "label": "作品", "visible": true },
    { "id": "packages", "label": "套餐", "visible": true },
    { "id": "about", "label": "关于我们", "visible": true }
  ]
}
```

---

### 4.16 用户/执行人表 `users`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | INT / BIGINT | 是 | 主键 |
| username | VARCHAR(50) | 是 | 登录账号，唯一 |
| password_hash | VARCHAR(255) | 是 | 密码哈希 |
| role | VARCHAR(20) | 是 | `admin` / `photographer` / `selector` / `finance` |
| name | VARCHAR(50) | 否 | 显示姓名 |
| created_at | DATETIME | 是 | 创建时间 |

---

## 5. B 端后台页面结构

### 5.1 全局布局组件
- `AppLayout`：左侧 `Sidebar` + 顶部 `Breadcrumb` + 右侧 `MainContent`。
- `Sidebar`：固定 210px，菜单项图标 + 文字，选中项左侧 3px `#409EFF` 竖线 + 浅蓝背景。
- `PageHeader`：页面标题 + 面包屑（如「工作台 > 套系」）。
- `EmptyPlaceholder`：占位菜单页面，显示「功能开发中」图标 + 提示文字。

---

### 5.2 工作台（占位）
仅展示占位页：
- 顶部问候语 + 今日日期。
- 4 个数据卡（总订单数、本月营收、待拍摄数、待选片数）—— 数据可接真实统计接口，也可写死示例。
- 一个「功能开发中」提示区。

---

### 5.3 小程序（占位）
占位页：
- 提示「小程序管理功能开发中」。
- 显示小程序 AppID、版本号、体验版二维码占位图。

---

### 5.4 资料设置（占位）
参考截图结构，分 Tab 占位：
1. **基本信息**：头像、工作室名称、简介、地区、地址、团队成员介绍。
2. **封面/轮播图**：封面上传列表、轮播图上传列表、排序。
3. **视频及故事动态**：视频/图文动态上传区（占位）。
4. **上传内容**：跳转到套系/客片/档期管理的快捷入口列表。
5. **服务协议**：服务协议富文本编辑器（占位，可保存纯文本）。

> 当前阶段仅实现「保存」按钮的表单占位，数据写入 `settings` 表；不接入实名/微信认证等付费流程。

---

### 5.5 套系管理（P0）

#### 5.5.1 列表页 `/packages`
顶部区域：
- 标题「套系」。
- 搜索框：按套系名称搜索。
- 筛选下拉：分类、状态（全部/上架/下架）。
- 排序下拉：默认排序 / 价格升序 / 价格降序。
- 主按钮：「+ 新建套系」。
- 高级选项下拉：批量上下架（占位）、导出 Excel。

列表内容（卡片或表格）：
- 封面图、套系名称、分类、价格、定金、状态（上架/下架）。
- 操作按钮：分享（占位）、编辑、下架/上架、删除。

#### 5.5.2 新增/编辑弹窗（抽屉）
使用 `el-drawer` 或 `el-dialog`，宽度 720px，底部固定保存/取消。

Tab 1：套系名称
- 套系封面（`el-upload`，单图）。
- 套系名称（输入框，必填）。
- 套系分类（选择器，可跳转管理分类）。
- 详情图片（多图上传，最多 80 张占位）。
- 套系视频（占位，不上传）。

Tab 2：价格及问卷
- 服务参数（选择器：单规格服务，占位）。
- 价格（¥）。
- 定金（¥）。
- 显示货币（人民币，占位）。
- 退订政策（选择器：严格/宽松，占位）。
- 底片保存（原片/精修保存期限，占位）。
- 预支付开关（占位）。
- 客户问卷开关（不显示/支付后/预约后，占位）。
- 验证手机号开关（占位）。

Tab 3：服务及加片
- 拍摄时长（选择器：全天/半天/小时）。
- 底片数量（输入框 + 「底片全送」复选框）。
- 精修片数量。
- 加片费（单价）。
- 加片优惠（占位）。
- 服装/化妆/相册（单选：不提供/提供，占位）。
- 服务地点（选择器 + 输入框）。
- 自定义服务详情（富文本）。

Tab 4：其他详情
- 对外公开开关 + 可见范围（全部可见，占位）。
- 咨询提醒开关（占位）。
- 温馨提示（多行文本）。
- 标签（多选：婚纱类、婚礼类等）。
- 顾客协议（占位）。

保存行为：
- 校验必填。
- 写入 `packages` 表。
- 返回列表刷新。

---

### 5.6 客片（作品库）（P0）

#### 5.6.1 列表页 `/works`
顶部区域：
- 标题「客片」。
- 主按钮：「+ 新建客片」。
- 次级按钮：「+ 图片直播」（占位）。
- 筛选：展示状态（全部/公开/非公开）、分类、排序（展示顺序/创建时间）。
- 搜索框：按客片名称搜索。
- 高级选项：导出 Excel（占位）。

列表内容：
- 瀑布流/网格卡片：封面图、标题、拍摄日期、分类、浏览量（占位）、点赞（占位）。
- 悬停显示操作：分享、置顶（占位）、统计（占位）、更多（编辑/删除）。

#### 5.6.2 客片详情/编辑页 `/works/:id`
左侧：
- 客户预览手机模型（占位，显示封面 + 标题 + 简介）。
- 返回列表按钮。

右侧：
- 图片上传区（支持多选拖拽上传）。
- 图片网格：缩略图、设封面、删除、拖拽排序。
- 基本信息表单：标题、分类、是否对外展示开关、作品说明。
- 底部保存 / 取消。

---

### 5.7 档期管理（日历模块）（P0）

#### 5.7.1 页面布局 `/schedule`
- 左侧：月历日历视图（`el-calendar` 或自定义）。
- 右侧：悬浮/固定面板，展示选中日期详情。

#### 5.7.2 月历视图
- 顶部年月选择器（`<` `>` 切换，年/月下拉）。
- 图例：黄色 = 未付定金，绿色 = 等待拍摄。
- 每个日期格子：
  - 公历日期（大号）。
  - 农历日期（小号灰色）。
  - 状态标签（档期已关闭 / 未付定金 / 等待拍摄 / 空闲）。
  - 若有订单，显示订单客户名或订单号前缀。

#### 5.7.3 右侧选中日期面板
- 显示「2026年8月 / 六月廿三」。
- 无档期时显示「无档期安排」+「+ 添加档期」按钮。
- 有档期时展示档期列表：时段、状态、订单、摄影师、操作（编辑/删除/关闭）。

#### 5.7.4 添加/编辑档期弹窗
- 日期（自动带入）。
- 时段（全天/上午/下午）。
- 状态（空闲/已约/关闭）。
- 绑定订单（选择器，可选）。
- 摄影师。
- 备注。

#### 5.7.5 高级选项下拉
- 档期及预约设置（占位）。
- 导出 Excel 档期报表（导出 `schedules` 表）。

---

### 5.8 订单中心（P0 核心）

#### 5.8.1 列表页 `/orders`
顶部操作区：
- 主按钮：「+ 添加新订单」蓝色。
- 筛选下拉：订单状态、执行者、订单类型、渠道来源。
- 拍摄时间区间选择器。
- 搜索框：客户名、套系名、备注。
- 排序下拉：最近操作 / 创建时间。
- 高级选项下拉：
  - 订单 & 选片全局设置（占位）。
  - 照片保存期限全局设置（占位）。
  - 导出 Excel 全部订单。
  - 订单回收站（软删除列表）。

预警提示栏：
- 黄色提示：图片即将到期（0）。
- 红色提示：选片超时（0）。
- 右侧「设置」入口（占位）。

订单卡片列表：
- 卡片头部：客户名字 + 编辑图标、订单编号、订单状态标签。
- 卡片主体：套系封面、套系名称、金额、默认好评时间、照片过期状态。
- 卡片底部：执行者头像、操作按钮（查看订单、分享订单）。

#### 5.8.2 新增订单弹窗
表单字段：
- 客户信息：客户姓名（必填）、联系电话（必填）。
- 拍摄日期 + 时段选择器，可勾选「日期待定」。
- 关联套系（选择器，必填）—— 选择后自动回填：套系价格、定金、精修、底片、拍摄时长。
- 财务：套系价格（只读/可微调）、定金、收款状态（未收/已收）。
- 其他消费：动态表格，可添加多行（项目名称、单价、数量、小计）。
- 拍摄地点。
- 备注。
- 渠道来源。
- 执行人选择。

保存联动：
1. 生成订单编号 `NO` + 时间戳。
2. 写入 `orders` 表，状态 `unpaid`。
3. 若选择了拍摄日期且非日期待定，在 `schedules` 表写入/更新一条记录：`date=shoot_date`，`order_id=订单ID`，`status='unpaid'`（黄色）。

#### 5.8.3 订单详情页 `/orders/:id`
顶部区域：
- 订单编号。
- 主按钮：「分享客户影集」（生成二维码）。
- 次级按钮：「修改订单状态」「编辑订单」「加片设置」「更多设置」。
- 业务进度时间轴：支付订单 → 完成拍摄 → 上传底片 → 完成，显示各节点时间。

客户 & 订单信息区块：
- 客户头像（姓名首字）、客户姓名、客户信息标签、电话。
- 操作按钮：编辑订单、加片设置、更多设置。
- 套系信息卡片：套系名称、定金、尾款、已付加片费、拍摄时间、拍摄地址、渠道来源、执行人。
- 业务明细：总价、服务详情、底片政策、加片费、拍摄时长、预估张数、精修数量、备注。

存储提示栏：
- 原片剩余保存期、精修剩余保存期。
- 设置保存时长按钮（弹窗设置 `raw_storage_days`、`retouch_storage_days`，并自动计算过期时间）。

#### 5.8.4 订单回收站
- 列表展示 `is_deleted=1` 的订单。
- 操作：恢复（`is_deleted=0`）、彻底删除（物理删除，仅 admin）。

---

### 5.9 客户管理（占位）
占位页：
- 提示「客户管理功能开发中」。
- 可展示客户总数占位。

---

### 5.10 容量管理（占位）
占位页：
- 提示「容量管理功能开发中」。
- 可展示存储用量进度条（占位图）。

---

### 5.11 数据统计（占位）
占位页：
- 提示「数据统计功能开发中」。
- 可展示营收/订单趋势占位图表。

---

## 6. C 端小程序页面结构

### 6.1 页面总览
| 页面 | 入口 | 说明 |
|---|---|---|
| 首页 | tabBar 首页 | 浏览公开客片、查看可预约档期 |
| 作品列表 | 首页/Tab | 公开客片瀑布流 |
| 套餐列表 | tabBar 套餐 | 公开套系列表 |
| 我的订单 | tabBar 我的 | 客户授权后查看个人订单 |
| 订单详情 | 我的订单进入 | 订单信息、进度、存储提示 |
| 在线选片 | 订单详情进入 | 对已拍摄订单的样片进行选片 |
| 影集查看 | 订单详情进入 | 查看交付的精修/成片影集 |
| 预约页 | 首页/套餐页进入 | 提交拍摄预约 |

> tabBar 建议：「首页」「作品」「套餐」「我的」。其中「我的」聚合「我的订单」「联系摄影师」「关于我们」。

### 6.2 首页
- 顶部轮播 Banner（读取 `settings.studio.cover`）。
- 工作室简介区（读取 `settings.studio.name` / `intro`）。
- 公开客片横向滚动卡片。
- 「查看全部作品」入口。
- 可预约档期日历预览（本月可约日期高亮，关闭日期置灰）。
- 「立即预约」悬浮按钮。

### 6.3 作品列表页
- 顶部分类 Tab。
- 瀑布流/双列网格展示 `works` 中 `is_public=1` 的作品。
- 点击进入作品详情：大图轮播 + 作品说明。

### 6.4 套餐列表页
- 分类筛选 Tab。
- 套系卡片：封面、名称、价格、定金、标签。
- 点击进入套餐详情：价格、服务说明、温馨提示、「咨询预约」按钮（复制微信号/拨打电话，不调用微信支付）。

### 6.5 我的订单页
- 登录/授权提示（未登录时）。
- 订单列表卡片：套系封面、套系名、金额、状态。
- 点击进入订单详情。

### 6.6 订单详情页
- 进度条：支付 → 拍摄 → 选片 → 精修 → 交付 → 完成。
- 订单信息：套系、拍摄时间、地点、金额、已付。
- 根据状态显示操作入口：
  - 状态 `shot` 后：「在线选片」按钮。
  - 状态 `delivered` 后：「查看影集」按钮。
  - 全部状态：「联系摄影师」。

### 6.7 在线选片页
- 顶部：已选 X / Y 张。
- 网格展示该订单关联作品的 `sample` 小样照片。
- 点击图片切换选中状态。
- 底部：「保存草稿」「提交选片」。
- 提交后写入 `photo_select`，B 端订单状态可自动变为 `retouching`。

### 6.8 影集查看页
- 网格展示 `final` 成片照片。
- 顶部：是否允许下载（读取 `works.allow_download`）。
- 若允许：长按/点击保存图片。
- 底部：「分享给亲友」（占位，使用微信小程序原生转发）。

### 6.9 预约页
- 表单：称呼、电话、意向套系（选择器）、期望日期（日期选择器，禁用 `schedules.is_closed=1` 的日期）、备注。
- 提交后写入 `appointments`，B 端预约管理可见。

---

## 7. 跨模块业务联动逻辑

### 7.1 创建订单联动
1. 用户选择套系 → 前端从 `packages` 读取价格/定金/服务参数回填表单。
2. 保存订单时：
   - `orders.package_snapshot` = 套系当前信息 JSON 快照。
   - `orders.package_price`、`deposit`、`balance` 自动计算。
   - 若指定拍摄日期且非日期待定：
     - 检查 `schedules` 该日期是否 `is_closed=1`，若是则提示「该日期已关闭，请更换」。
     - 写入/更新 `schedules`：`order_id` = 新订单 ID，`status='unpaid'`（黄色）。
3. 订单创建成功，返回订单详情。

### 7.2 订单收款联动
1. B 端登记收款 → 写入 `payments` 流水。
2. 重算 `orders.paid_amount`。
3. 若收到定金且原状态为 `unpaid`，自动将 `orders.status` 改为 `deposit`。
4. 若尾款收齐，`orders.payment_status` 改为 `paid`。

### 7.3 拍摄完成联动
1. B 端将订单状态改为 `shot`。
2. 系统自动记录 `orders.shot_at`。
3. 该订单关联作品上传 `sample` 小样后，C 端订单详情出现「在线选片」入口。
4. `schedules` 中对应日期状态同步为 `shoot`（绿色）。

### 7.4 在线选片联动
1. C 端进入在线选片页，调用 `GET /api/customer/photo-select/:orderId`，仅返回该订单自己的数据。
2. 客户勾选照片，可「保存草稿」或「提交选片」。
3. 提交后 `photo_select.submitted=1`，B 端订单中心显示「选片已提交」。
4. B 端可将订单状态推进为 `retouching`。

### 7.5 档期关闭/开放联动
1. B 端设置某日 `is_closed=1`。
2. C 端预约页的日期选择器实时禁用该日期。
3. 若该日期已绑定订单，B 端给出警告但不强制解绑；C 端仍按订单展示。

### 7.6 评价联动
1. C 端在订单完成后提交评价 → 写入 `evaluates`，`status='pending'`。
2. B 端「评论管理」（占位）可审核，审核通过后 `status='approved'`。
3. 通过的评价可在小程序首页「好评墙」展示。

### 7.7 订单软删除与回收站
1. B 端删除订单 → `orders.is_deleted=1`，`deleted_at=now()`。
2. 列表页只展示 `is_deleted=0`。
3. 回收站展示 `is_deleted=1`。
4. 恢复订单 → `is_deleted=0`。
5. 彻底删除 → 物理删除（仅管理员，同时级联删除 `payments`、`photo_select`、`order_extras`）。

### 7.8 分享订单二维码
1. B 端点击「分享客户影集」→ 生成唯一 `share_token`。
2. 生成小程序码/二维码图片，展示给摄影师保存发送给客户。
3. 客户扫码进入小程序订单详情/影集页，后端根据 token + wx.login 的 openid 校验是否匹配该订单；若匹配则展示，否则提示「无权查看」。

### 7.9 存储期限计算
- 原片过期时间：`raw_expire_at = uploaded_at + raw_storage_days 天`。
- 精修片过期时间：`retouch_expire_at = delivered_at + retouch_storage_days 天`。
- 订单详情页展示剩余天数，到期前 7 天预警。

---

## 8. 接口设计（面向低代码平台，非完整代码）

### 8.1 认证
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/login` | B 端登录，返回 token |
| GET | `/api/auth/me` | 获取当前用户 |

### 8.2 套系
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/packages` | 列表（支持 status/category/q/sort） |
| GET | `/api/packages/:id` | 详情 |
| POST | `/api/packages` | 新增 |
| PUT | `/api/packages/:id` | 编辑 |
| DELETE | `/api/packages/:id` | 删除 |
| POST | `/api/packages/:id/toggle` | 上下架 |
| GET | `/api/packages/export` | 导出 Excel |

### 8.3 客片
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/works` | 列表 |
| GET | `/api/works/:id` | 详情 |
| POST | `/api/works` | 新增 |
| PUT | `/api/works/:id` | 编辑 |
| DELETE | `/api/works/:id` | 删除 |
| POST | `/api/works/:id/photos` | 上传图片 |
| PUT | `/api/works/:id/cover` | 设置封面 |
| DELETE | `/api/photos/:id` | 删除单张图片 |
| GET | `/api/works/export` | 导出 Excel |

### 8.4 档期
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/schedules` | 月历数据（按年月） |
| POST | `/api/schedules` | 添加/更新档期 |
| DELETE | `/api/schedules/:id` | 删除档期 |
| GET | `/api/schedules/export` | 导出 Excel |

### 8.5 订单
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/orders` | 列表（支持 status/executor/channel/date/search） |
| GET | `/api/orders/:id` | 详情（含收款流水、其他消费） |
| POST | `/api/orders` | 新增 |
| PUT | `/api/orders/:id` | 编辑 |
| DELETE | `/api/orders/:id` | 软删除 |
| POST | `/api/orders/:id/restore` | 恢复 |
| POST | `/api/orders/:id/payments` | 登记收款/退款 |
| POST | `/api/orders/:id/status` | 修改订单状态 |
| POST | `/api/orders/:id/share` | 生成分享二维码 |
| GET | `/api/orders/export` | 导出 Excel |
| GET | `/api/orders/recycle` | 回收站列表 |

### 8.6 选片（B 端管理）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/selections` | 选片中订单列表 |
| GET | `/api/admin/photo-select/:orderId` | 查看某订单选片 |
| POST | `/api/admin/photo-select/:orderId` | 代客修改选片 |

### 8.7 C 端小程序
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/settings/studio` | 工作室资料（公开） |
| GET | `/api/customer/works/public` | 公开客片 |
| GET | `/api/customer/packages/public` | 公开套系 |
| GET | `/api/customer/schedules/available` | 可预约档期 |
| POST | `/api/customer/appointment/submit` | 提交预约 |
| GET | `/api/customer/order/list` | 我的订单 |
| GET | `/api/customer/order/:orderId` | 订单详情 |
| GET | `/api/customer/photo-select/:orderId` | 读取选片 |
| POST | `/api/customer/photo-select/save` | 保存草稿 |
| POST | `/api/customer/photo-select/submit` | 提交选片 |
| GET | `/api/customer/album/:orderId` | 影集查看 |
| POST | `/api/customer/evaluate/submit` | 提交评价 |

---

## 9. Excel 导出字段定义

### 9.1 订单导出
```
订单编号, 客户姓名, 客户电话, 套系名称, 订单状态, 套系价格, 定金, 尾款, 已收金额,
其他消费合计, 拍摄日期, 拍摄时段, 拍摄地点, 渠道来源, 执行人, 创建时间,
支付时间, 完成拍摄时间, 上传底片时间, 完成时间
```

### 9.2 档期导出
```
日期, 农历, 时段, 状态, 档期关闭, 订单编号, 客户姓名, 摄影师, 备注
```

### 9.3 套系导出
```
套系名称, 分类, 售价, 定金, 精修张数, 底片政策, 拍摄时长, 状态, 小程序上架, 排序, 创建时间
```

---

## 10. 安全与权限

### 10.1 B 端角色
| 角色 | 权限 |
|---|---|
| admin | 全部 |
| photographer | 作品、档期、订单（除删除）、财务查看 |
| selector | 在线选片管理 |
| finance | 订单收款/退款、导出 Excel、财务查看 |

### 10.2 C 端安全
- 所有 `/api/customer/*` 接口必须携带客户 token。
- token 由 `wx.login` 换取 openid 后后端签发。
- 订单/相册/选片严格按 openid 行级隔离，越权返回 403。
- 作品/成片接口只返回 `sample` / `final` 分区，绝不返回 `local` 原片。

---

## 11. 低代码平台生成建议

1. **数据库**：先在低代码平台按第 4 节建表，建议主键自增 INT/BIGINT。
2. **B 端页面**：优先生成「套系」「客片」「档期」「订单中心」4 个模块；其余菜单可先创建占位页。
3. **表单**：套系编辑用 4 Tab 表单；订单新增用弹窗 + 动态子表（其他消费）。
4. **日历**：使用低代码平台日历组件，自定义日期单元格渲染颜色和标签。
5. **C 端小程序**：用平台小程序生成器创建首页、作品、套餐、我的 4 个 tabBar 页面，再创建订单详情、选片、影集 3 个子页面。
6. **Excel 导出**：使用低代码平台「导出」动作或后端自定义接口返回 CSV/Excel。
7. **图片存储**：开发期可用本地/平台存储；生产建议接入 Cloudflare R2 私有桶 + Worker 代理（参考已有方案），前端不暴露密钥。

---

## 12. 与现有代码的关系

本方案与当前 `摄影工作室管理系统/` 仓库已有的 React + Node monorepo 对应关系：
- 套系、客片、档期、订单、收款流水、选片、评价、设置等表与接口已大部分实现。
- 本方案可作为**低代码平台重新搭建**或**现有系统升级补齐**的需求原型。
- 若基于现有系统落地，只需按本方案扩展字段（如订单加 `is_deleted`、回收站、存储期限、分享 token）并补齐 UI 交互。
