# 摄影工作室管理系统 · 上线前自检测试报告
> 生成时间：2026-08-05 18:53 · 检测方式：对真实代码库静态扫描 + 后端冒烟（sqlite 本地模式）
> 说明：原提示词要求生成 Vue 测试组件，但本栈为 React + Express，已改为对**现有真实代码**执行等价检测逻辑，结果更可信。

---

## 测试1：图片存储链路检测（最高优先级）
**检测逻辑（实际执行）**
- 扫描 `server/` 下 `fs.writeFile / createWriteStream / renameSync / appendFile` 是否用于图片持久化
- 核对 `client/src/api.js` `uploadImage` 的上传目标与 `render.yaml` 的 `VITE_UPLOAD_WORKER_URL` 是否配置
- 提取 `storage.js` 返回 URL 的域名，比对是否为 `r2.dev`

**运行输出**
| 检测项 | 结果 | 证据 |
|---|---|---|
| 本地磁盘持久化图片 | ✅ 无风险 | `server/` 仅 `db.js:10`(sqlite数据目录) 与 `index.js:43`(multer临时目录) 用 `fs.mkdirSync`；图片走 R2 SDK 转发后 `fs.unlinkSync` 删除临时文件，**无任何 `writeFile/renameSync` 落盘图片** |
| 返回图片 URL 域名 | ✅ 非 r2.dev | `storage.js:72` → `${R2_WORKER_DOMAIN}/r2/${key}`；部署值 `yezhe-img-proxy.yezhe128627.workers.dev`（Worker 代理域名） |
| 全局 r2.dev 硬编码 | ✅ 0 处 | 全仓库 grep `r2.dev` → 无匹配 |
| 二进制是否还提交 Render | ⚠️ 当前走 Render 中转 | `render.yaml` 未配置 `VITE_UPLOAD_WORKER_URL`，前端走 `/api/upload` 回退，二进制经 Render 转发 R2（非纯直传 Worker） |

**测试报告模板填写**
- 域名类型：`workers.dev` 自动子域（非 r2.dev，但**也非完全自定义 CDN 域名**如 cdn.yezhe.com）
- 本地磁盘写文件风险：**低（已消除）** — 图片持久化在 R2，Render 重启不丢图
- 待办（非阻断）：部署 `cloudflare/upload-worker.js` 并配置 `VITE_UPLOAD_WORKER_URL` + `UPLOAD_TOKEN` 后，二进制才真正全部直传 Worker（T-01 验收标准#1 完全达成）；建议后续给 Worker 绑自定义域名替换 `*.workers.dev`

---

## 测试2：容量管理模块可用性检测
**检测逻辑**
- 读取 `CapacityManagement.jsx` 阈值函数与 TABS 定义
- 核对 `Sidebar.jsx` 菜单、`App.jsx` 路由、`Dashboard.jsx` 首页卡片联动
- 全局扫描 VIP/付费/扩容 正向文案

**运行输出**
| 检测项 | 结果 | 证据 |
|---|---|---|
| 阈值逻辑 | ✅ 通过 | `CapacityManagement.jsx:24-25` → `ratio>=0.9 critical(红)`、`>=0.7 warning(黄)`、否则 `normal(绿)`，与规范一致 |
| 侧边栏菜单 | ✅ 通过 | `Sidebar.jsx:33-35` → 分组「容量管理」+ `/capacity` + `storage` 图标 |
| 三 Tab 渲染 | ✅ 通过 | `CapacityManagement.jsx:6-9` → 存储空间 / 图片流量 / 视频流量(占位)；`:399-410` 视频流量占位说明 |
| 首页告警同步 | ✅ 通过 | `Dashboard.jsx:145` 存储状态卡片；`:111` critical 时「去清理 →」按钮 |
| VIP/付费 UI | ✅ 无 | 全仓库 grep `VIP/付费/扩容/会员/升级` 仅命中反付费声明，无正向付费入口 |
| 5-15min 延迟标注 | ✅ 通过 | 页面顶部时延提示文案已内置 |

**测试结果报告**：容量管理模块全部可用项 **PASS**，无风险。

---

## 测试3：删除操作安全检测（防误删客片）
**检测逻辑**
- 扫描前端 `confirm()` 文案是否包含「永久删除，请先本地备份」
- 扫描后端是否存在定时/自动删除业务图片逻辑（`setInterval` 清理图片等）
- 核对容量批量清理删除前是否导出清单 JSON

**运行输出**
| 检测项 | 结果 | 证据 |
|---|---|---|
| 删除二次确认文案 | ✅ 通过 | `Orders.jsx:154` `WorkDetail.jsx:154` `Packages.jsx:88,367` `Reviews.jsx:30` `Schedule.jsx:84` 全部为「确认后将永久删除，建议先做好本地备份，确定继续？」 |
| 自动定时删图逻辑 | ✅ 无（高危规避） | 后端 `setInterval` 仅用于 `backup.js` 每日备份；**无任何自动删除业务图片代码** |
| 容量批量清理导出清单 | ✅ 通过 | `CapacityManagement.jsx` 导出 `delete-list.json`（替代原 CSV），删除前必须勾选+双确认 |
| 订单彻底删除 | ✅ 受控 | `Orders.jsx` 删除带确认弹窗且为 admin 权限接口 |

**风险检测报告**
- 高危项：**0**
- 低风险项：**0**
- 结论：删除安全链路完整，无任何一键直删或自动删图逻辑。

---

## 测试4：微信小程序 C 端性能 & 图片检测
**检测逻辑**
- 小程序目录 grep `r2.dev` / `stores`
- 统计 `onUnload` 覆盖页面数
- 检查 `image` 是否带 `lazy-load`、是否请求缩略图参数（`?w=`）
- 读取 `app.json` 分包配置

**运行输出**
| 检测项 | 结果 | 证据 |
|---|---|---|
| r2.dev 硬编码 | ✅ 通过 | miniprogram 全目录 grep `r2.dev`/`stores` → 无匹配 |
| 分包配置 | ✅ 通过（结构上） | `app.json` → 主包 `pages/`(4 tab 页) + 分包 `pkg/`(11 页)；tabBar 仅用主包 |
| 图片懒加载 | ⚠️ 部分 | 多数 `image` 带 `lazy-load`；但 `album.wxml:21`、`photoSelect.wxml:10` 缺 `lazy-load` |
| 缩略图/原图分离 | ❌ 未实现 | 全小程序无 `?w=`/`thumb` 请求；Worker 虽支持 `buildThumbKey(?w=)` 但前端未用，列表与详情同用原图 URL（T-16 部分不通过） |
| onUnload 生命周期 | ❌ 部分（7 页缺失） | 15 页中 8 页有 `onUnload`（index/my/package/works/about/packageDetail/schedule/workDetail）；**缺：album、appointment、evaluate、order、photoSelect、share、subscribe**（T-17 不通过） |
| 首包体积 ≤1.5M | ⏳ 待实测 | 主包仅 4 tab 页 + utils，结构上应达标，但需用开发者工具「代码依赖分析」实测确认（T-15 待验证） |

**检测报告结论**
- 通过：r2.dev 清除、分包结构、懒加载（多数）
- 不通过：缩略图分离（T-16）、onUnload 全覆盖（T-17）
- 待实测：首包体积（T-15）

---

## 测试5：数据备份与数据库字段校验检测
**检测逻辑**
- 扫描 `server/src` 是否存 `base64 / data:image / Blob / Buffer` 进库
- 核对手动导出 JSON 备份接口与前端按钮
- 核对定时任务写 R2 `/backup`

**运行输出**
| 检测项 | 结果 | 证据 |
|---|---|---|
| DB 图片字段仅存 http url | ✅ 通过 | grep `base64/data:image/Blob/Buffer` → 仅 `storage.js:5` 负向注释「绝不存 base64/blob」，无实际入库二进制 |
| 手动导出全量 JSON 备份 | ✅ 通过 | 后端 `GET /api/admin/backup/export`（blob） + 前端 `Settings` 页【导出 JSON 备份】按钮（`api.js downloadBackup`） |
| 定时写 R2 /backup | ✅ 通过 | `backup.js:64 scheduleDailyBackup`（每日 03:10，`setTimeout+setInterval`）+ `POST /api/admin/backup/run` 手动触发；`index.js:113` 启动调度 |
| 备份内容脱敏 | ✅ 通过 | `buildFullBackup` 已过滤明文密钥字段 |

**风险报告**
- 风险项：**0**
- 注意：定时备份依赖 R2 配置（`R2_*` 五项）；当前生产 R2 已配，备份将自动写入 `/backup/yezhe-backup-*.json`。

---

# 综合结论与上线建议
**🔴 阻塞项现状**：T-01/T-02(代码层)/T-03/T-04/T-05/T-06/T-07/T-08/T-09/T-10/T-18/T-20 均已落地并通过检测；唯一未完全达成的验收标准是 **T-01 验收#1（二进制纯直传 Worker）** 与 **T-02 完全自定义 CDN 域名**——两者均为**部署动作**（代码已就绪），不影响"重启不丢图"的核心安全目标。

**待完成（建议单独一轮）**：T-11 长列表虚拟滚动；T-15 首包体积实测；T-16 小程序缩略图分离；T-17 补全 7 个页面的 onUnload。

**上线前必做**：`git push` 触发 Netlify/Render 构建；部署 `upload-worker.js` 并配置 `VITE_UPLOAD_WORKER_URL`/`UPLOAD_TOKEN`；Test-1 真机重启破坏性验证。
