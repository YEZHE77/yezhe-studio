# C 端 H5 验收测试用例清单 · 代码审计报告

- **审计时间**：2026-08-29
- **代码基线**：HEAD `8d99a20`
- **审计方式**：逐条比对清单要求与 `client/src`（页面/组件/utils）及 `server/src/routes`（share / customer / customerMine / queryOrder）实现
- **总体结论**：**60 条用例中，完全通过约 12 条，部分通过约 11 条，未通过约 37 条**；黑名单 7 项中命中 5 项

图例：✅ 通过 ｜ ⚠️ 部分通过（功能有但文案/交互/细节不符）｜ ❌ 未通过

---

## 前置约束 · 微信环境顶部常驻提示条

| 要求 | 结果 | 说明 |
|---|---|---|
| 微信内所有 C 端页面顶部常驻「此链接为系统专属访问地址…」 | ❌ | 该文案目前**只是** `Settings.jsx:345` 中 `orderShareNote` 的 input `placeholder`（B 端「订单分享备注」配置项），**并非 C 端组件**。微信 UA 检测仅散落在 `CustomerSelectPhoto` / `MobileWorkbench` / `MobileMessage` / `OrderDetail` 四处，无全局常驻横幅 |

---

## 一、主流程业务（9 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 1.1 | 手机号 A 登录可见其预约/订单/相册 | ✅ | `customerMine.js:200-205` 匹配 `reservations.phone` / `orders.customer_phone` |
| 1.2 | 第二绑定手机号 B 登录同样可见 | ✅ | 同上，匹配 `phone_two` |
| 1.3 | 解绑后无法访问原订单 | ✅ | 登录查询走当前手机号字段，解绑即查不到 |
| 1.4 | 状态文案客户化，不暴露后端枚举 | ✅ | 返回 `status_label`，源自 `RESERVATION_STATUS` / `ORDER_STATUS` 映射 |
| 1.5 | 预约表单提交成功，后台收到记录 | ✅ | `AppointmentForm.jsx` → `/api/customer/reservation-submit` |
| 1.6 | 后台状态变更，C 端刷新后同步 | ⚠️ | 数据每次进页面会重拉，但**无手动刷新按钮**（见 8.1） |
| 1.7 | 预约转订单后 C 端出现对应订单 | ✅ | `my-business` 返回 `order_token`，卡片有「查看对应订单 ›」 |
| 1.8 | 公开样片合集 share-key 正常打开 | ✅ | `ShareAlbum` / `WorkPublic` |
| 1.9 | **客户专属相册禁止 share-key 访问** | ❌ **P0** | `share.js:251-258` `buildPayload` 支持 `case 'order'`；且 `legacyOrderShare()`（264-273）用 `orders.share_token` 直接出订单影集 → 专属相册可凭分享链接直开 |

## 二、登录 & 鉴权（7 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 2.1 | 未登录访问受保护 URL，保存并登录成功回跳原页面 | ❌ **P0** | 全前端**无** `redirect` / `returnTo` / `next` 参数机制（grep 零命中）；`CustomerLogin.jsx:53` 固定 `nav('/customer/mine', {replace:true})` |
| 2.2 | 不存在订单的手机号：友好提示、保留输入、不白屏 | ✅ | 返回 403 业务文案，错误渲染在输入框下方，表单不清空 |
| 2.3 | Token 篡改/过期：清除状态 + 提示过期 + 跳登录 | ⚠️ | `/me` 失效时前端仅静默置为未登录（`CustomerMine.jsx:78`），**无「登录过期」提示、无跳转** |
| 2.4 | 越权访问他人订单：提示「您无权查看该内容」，不泄露是否存在 | ❌ **P0** | `customerMine.js:322` 无效 token → 404 `订单不存在`；`queryOrder.js:169` 非本人 → 404 `订单不存在`、171 → 403 `无权查看该订单`。**均泄露「订单是否存在」**，且文案非统一 `您无权查看该内容` |
| 2.5 | 手机号前后空格自动过滤 | ✅ | `phone.trim()` |
| 2.6 | 格式错误就近提示，不弹全局弹窗 | ✅ | 内联红字块，非 `alert`/全局弹窗 |
| 2.7 | 同手机号多设备同时登录不踢下线 | ❌ | `customerMine.js:215` `UPDATE customer_user SET session_id = ?, session_expire_at = ?`（**单会话/手机号**），新设备登录覆盖旧 session |

## 三、分享链接 share-key（5 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 3.1 | 正确 share-key 正常打开 | ✅ | `share.js:292` `GET /api/share/:token` |
| 3.2 | 篡改 key → 「该分享链接已失效，请向摄影师获取最新链接」 | ⚠️ | 实际返回 404 `分享链接无效或已失效`，副文案「…请联系摄影师获取最新链接。」**非清单指定文案** |
| 3.3 | `is_share_open=false` → 「该合集已关闭对外分享」 | ❌ | **无 `is_share_open` 字段**（全库 grep 零命中）；实际用 `shares.disabled`，文案 `该分享已被关闭` |
| 3.4 | 重置 share-key 后旧链接失效 | ⚠️ | 机制可用（token 查不到即 404），文案同 3.2 不符 |
| 3.5 | 参数丢失/乱码 → 「链接格式不正确，请复制完整链接打开」 | ❌ | 无该分支；`/api/share/undefined` 落到 404 通用错误 |

## 四、客户订单相册链接（2 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 4.1 | 篡改/删除参数 → 「链接无效，请通过手机号登录查看您的专属相册」+跳登录 | ❌ | `customerMine.js:320` 返回 400 `缺少 accessToken`（原始后端文案），**无跳转登录** |
| 4.2 | 客户相册不接受 share-key，传入直接走登录流程 | ❌ **P0** | 同 1.9：`/share/:token` 可直接渲染 `payload.order`（订单影集） |

## 五、异常兜底（7 条 · 重点）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 5.1 | 断网顶部悬浮提示 + 按钮置灰 + 恢复自动解除 | ❌ | **全前端无** `navigator.onLine` / `online`/`offline` 监听（grep 零命中） |
| 5.2 | 超时弹窗 + 重试按钮 | ⚠️ | `api.js:45-53` 有 GET 自动重试 2 次与 toast，但**无「重试」按钮** |
| 5.3 | 500/502/503 友好提示，不输出错误码/堆栈 | ❌ **P0 系统性** | 后端 **302 处** `res.status(500).json({ error: e.message })`（覆盖 37 个路由文件）；前端 `api.js:75` `data.error \|\| ('请求失败('+status+')')` 直接 toast → 原始 SQL 报错可见 |
| 5.4 | 公开样片：服务异常时优先保证图片浏览 | ⚠️ | 未做「埋点失败不影响看图」的显式降级 |
| 5.5 | 客户订单/相册报错不降级，只提示重试 | ⚠️ | 无统一的「业务数据不降级」约束 |
| 5.6 | 空订单数组 → 「暂无订单记录」+「如有疑问，请联系摄影师」小字 | ⚠️ | `CustomerMine.jsx:275` 仅 `暂无订单`，**无小字** |
| 5.7 | 空预约数组 → 「暂无预约记录」 | ⚠️ | `CustomerMine.jsx:245` 为 `暂无预约`（文案少「记录」） |

## 六、相册图片资源（5 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 6.1 | 单图失效 → 统一占位图 + 「图片加载失败」，无原生裂图 | ❌ | `AlbumGrid.jsx:255` 网格视图仅 `onError → opacity=0.3`（**未替换占位图，裂图仍可见**）；`:274` 单列大图**无 onError** |
| 6.2 | 多图失效 → 顶部「部分图片加载异常，请检查网络或稍后刷新」 | ❌ | 无计数与顶部提示 |
| 6.3 | 大图加载骨架屏，不大片空白 | ❌ | 无 skeleton |
| 6.4 | 手机端左右滑动 + 捏合缩放；电脑端方向键翻图 | ⚠️ | `AlbumGrid` 为**纵向滚动**单列/双列，无灯箱、无缩放、无键盘翻页 |
| 6.5 | 图片懒加载 | ✅ | `loading="lazy"` |

## 七、预约表单提交（4 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 7.1 | 连点防重复提交 | ✅ | `submittingRef` 同步拦截 + `disabled={busy}` |
| 7.2 | 必填项为空就近提示 | ⚠️ | 错误统一渲染在**提交按钮上方**（`AppointmentForm.jsx:156`），非字段级就近 |
| 7.3 | 提交后状态不确定 → 「提交状态不确定，请稍后查看预约记录…」 | ❌ | `catch` 仅透传 `data.error` 或 `提交失败`，**无不确定态分支** |
| 7.4 | 提交成功清晰告知 + 可登录查看进度 | ✅ | 「提交完成，请等待摄影师确认」+「去我的页面查看预约进度」 |

## 八、业务边界（2 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 8.1 | 后台状态变更；C 端提供手动刷新按钮 | ❌ | `CustomerMine` 无刷新按钮 |
| 8.2 | 解绑后登录 → 「该手机号无对应订单权限」 | ⚠️ | 实际 `未找到该手机号对应的预约或订单，请确认手机号或联系摄影师`（语义对，文案不符） |

## 九、微信浏览器兼容（3 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 9.1 | 微信内顶部轻提示常驻 | ❌ | 同「前置约束」 |
| 9.2 | 不出现后端原始报错 | ❌ | 同 5.3 |
| 9.3 | 微信内登录回跳原页面 | ❌ | 同 2.1 |

## 十、性能体验（4 条）

| # | 用例 | 结果 | 依据 |
|---|---|---|---|
| 10.1 | 图片走 CDN，接口不传二进制 | ✅ | COS/R2 + `img()` 只传 URL |
| 10.2 | 列表图片懒加载 | ✅ | `loading="lazy"` |
| 10.3 | 公开合集 Redis 缓存；客户相册短 TTL | ❌ | **全后端无 Redis 依赖**（grep 零命中），需引入外部缓存服务（基建决策） |
| 10.4 | 弱网不卡死，有明确反馈 | ⚠️ | 有 toast 与重试，但无全局网络状态条 |

---

## 十一、BUG 黑名单命中情况

| # | 禁止现象 | 命中 | 说明 |
|---|---|---|---|
| 1 | 展示 403/500/error 堆栈/SQL 报错 | ❌ **命中** | 302 处 `e.message` 直出 + 前端直透传 |
| 2 | 鉴权后跳首页而非原页面 | ❌ **命中** | 固定跳 `/customer/mine` |
| 3 | 客户相册用 share-key 访问 | ❌ **命中** | `buildPayload('order')` + `legacyOrderShare` |
| 4 | 图片失败出现原生裂图 | ❌ **命中** | 仅降透明度，单列大图无处理 |
| 5 | 数据为空一片纯白无提示 | ✅ 未命中 | 有「暂无订单/暂无预约」 |
| 6 | 按钮可无限重复提交 | ✅ 未命中 | 预约表单有 ref + disabled 双保险 |
| 7 | 微信环境丢失顶部提示 | ❌ **命中** | 该提示条根本不存在 |

---

## 建议修复顺序（待确认）

**P0 · 安全与黑名单（必须先修）**
1. 禁止 share-key 访问客户专属相册：移除 `buildPayload` 的 `order` 分支与 `legacyOrderShare` 兜底（或改为强制跳手机登录）
2. 越权统一返回 `您无权查看该内容`（不区分「不存在」与「无权」）
3. 后端统一错误出口：新增 `serverError(res, e)` 只回 `服务暂时繁忙，请稍后再尝试`，同时 `console.error` 保留堆栈供排查；前端 `api.js` 对 5xx 不再透传 `data.error`
4. 会话改多设备并存（`customer_user` 一对多 session 表或 token 集合）
5. 微信顶部常驻提示条（C 端全局组件）

**P1 · 交互与文案**
6. 登录回跳（受保护页记录 `redirect` → 登录后回跳）
7. 全局网络状态条 + 断网按钮置灰
8. 图片统一占位图组件 + 多图失效顶部提示 + 骨架屏
9. 空态文案对齐（「暂无订单记录」+「如有疑问，请联系摄影师」）
10. 表单字段级错误提示 + 「提交状态不确定」分支
11. C 端手动刷新按钮
12. 分享错误文案对齐清单

**需基建决策**
13. Redis 缓存（清单 10.3）：Render 免费版需外接 Redis（如 Upstash），涉及账号与费用，请确认是否引入
