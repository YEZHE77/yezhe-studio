# 摄影工作室管理系统 · 内部测试报告 & 上线准备清单

> 生成时间：2026-08-07 · 检测方式：本地后端（sqlite 隔离实例）接口冒烟 + 小程序静态自检 + H5 构建校验
> 基础栈：React+Vite+Tailwind(B端) / 微信原生小程序(C端) / Express+Postgres(生产)·node:sqlite(本地) / 腾讯云 COS（主）·Cloudflare R2（兜底）

---

## 一、内部测试结果（2026-08-07）

### 1.1 后端接口冒烟（本地 sqlite，端口 4555）

| 分类 | 接口 | 结果 |
|---|---|---|
| 健康检查 | `GET /api/health` | ✅ `{ok:true}` |
| 鉴权 | `POST /api/auth/login`（admin/admin123） | ✅ 签发 JWT |
| 鉴权 | `POST /api/wx/login`（DEBUG 兜底） | ✅ 返回 openid+customerId+token |
| B端读 | categories / works / packages / schedules / orders / stats / settings/studio / admin/storage(+stats+traffic) | ✅ 全部 200 |
| B端读 | finance/summary（财务页真实路径） | ✅ |
| B端读 | admin/customers（客户管理真实路径） | ✅ `[]` |
| B端写 | 建/改/删 works、建 schedules、建 packages、建 orders | ✅ 全部成功 |
| C端读 | appointment/list / order/list / evaluate/public / photo-select / album | ✅ 全部 200 |
| C端写 | appointment/submit / photo-select/save / evaluate/submit | ✅ `{ok:true}` |
| C端写 | appointment/cancel（非法 id） | ✅ 干净返回「预约不存在」（非 500） |
| 上传 | `POST /api/upload`（未配存储） | ✅ 优雅报错「未配置云端存储（COS/R2）…」，不落盘、不崩 |

### 1.2 小程序静态自检（`node miniprogram/selfcheck.cjs`）

- ✅ **34 / 34 通过**：7 个分包页面 onUnload 清理（终止请求 + 释放内存）、`getImageUrl` 三分支（thumb/preview/original）+ 空值/异常兜底完整。
- ✅ 缩略图分离已实现：首页轮播、作品封面、套系封面、相册、选片、画廊、分享页均调用 `getImageUrl(url,'thumb'/'preview')`（注：COS 域下 `?w=` 暂不生效，见第三节）。

### 1.3 H5 前端

- ✅ `client vite build` 通过（无编译错误）。
- ⚠️ 浏览器内交互（登录态、页面跳转、上传进度 UI）需人工用 `Cmd+Shift+R` 强刷后在 pages.dev 验证。

### 综合结论

**后端 + 业务逻辑层内部测试通过，无阻断项。** 唯一「未自动验证」的是依赖真实云凭证/真机的部分：COS 实际上传、微信真机登录（WX_SECRET）、真机图片加载。

---

## 二、上线前必做（手动步骤，逐项勾选）

### 2.1 Render 后端环境变量（控制台 → 该服务 → Environment）
- [ ] `DATABASE_URL`：Neon Postgres 连接串（已配则跳过；未配则数据随重启丢失）
- [ ] `WX_APPID` / `WX_SECRET`：小程序 AppID + AppSecret（**必填**，否则真机 C端 认不到真实客户，每次都是新游客）
- [ ] `COS_SECRET_ID` / `COS_SECRET_KEY` / `COS_BUCKET` / `COS_REGION` / `COS_CDN_DOMAIN`：填齐即自动切 COS（优先级高于 R2）
- [ ] `CORS_ORIGIN`：已修正为 `https://yezhe-studio.pages.dev`（render.yaml 已改，部署后生效）
- [ ] （可选）`CF_API_TOKEN` / `CF_ACCOUNT_ID`：仅 R2 模式下用于容量/流量统计；用 COS 后可不填

### 2.2 微信小程序后台（开发管理 → 开发设置 → 服务器域名）
- [ ] `request 合法域名`：加 `https://yezhe-studio-server.onrender.com`
- [ ] `downloadFile 合法域名`：加 `https://yezhe-studio-server.onrender.com` + 你的 `COS_CDN_DOMAIN`
- [ ] `miniprogram/utils/config.js` 的 `IMG_CDN` 同步填为 `COS_CDN_DOMAIN`

### 2.3 触发部署
- [ ] `git push origin main` → Render 自动重建后端、Cloudflare Pages 自动重建 H5
- [ ] 后端 `/api/health` 返回 200；H5 用强刷打开确认能登录后台

### 2.4 真机验收（微信开发者工具 → 预览 → 扫码，5 项，禁止仅用模拟器）
- [ ] 相册/作品图片不空白、详情可滑动
- [ ] 选片不卡、分享封面正常、二维码可保存
- [ ] 图片加载失败有灰色占位、不崩页
- [ ] C端预约提交 → B端收到并在档期生成订单、锁档
- [ ] 幻灯片自动播放 + BGM 开关正常

### 2.5 存量图迁移（如需保留旧 R2 图，可选）
- [ ] `node scripts/migrate-r2-to-cos.mjs --dry` 预览 → 确认无误后去 `--dry` 正式迁移

---

## 三、已知小优化点（非阻断，可上线后迭代）

1. **COS 缩略图**：小程序 `getImageUrl` 已追加 `?w=420/1080`，但 COS 默认不解析该参数（走原图），移动端列表仍加载全分辨率。后续可在 COS 控制台开启「图片处理」或改用 COS CI 规则；当前不影响功能。
2. **长列表虚拟滚动**（原 T-11）：作品/相册图片极多时未做虚拟滚动，目前分页/瀑布流已缓解，超大量级再优化。
3. **H5「我的」页**：Web 端无客户会话，菜单项点击提示「请在微信小程序查看」，属预期（C端主阵地在小程序）。

---

## 四、遗留未提交文件说明（不阻塞）

- `cloudflare/worker.js`：R2 图片代理 Worker，COS 方案下不再需要，保持未提交原样，勿动。
- 根目录若干 `*.html` / R2 配置手册：历史操作手册，未跟踪，可按需清理或保留。
