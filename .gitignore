# 依赖
node_modules/
client/node_modules/
server/node_modules/

# 本地数据（开发用 SQLite + 上传图片），不入库
server/data/
client/dist/
uploads/

# 环境变量（含密钥），绝不提交
.env
.env.local
server/.env

# 日志
*.log
.DS_Store

# 本地保活工作流（仅本地运行，绝不入库提交）
.github/workflows/keepalive.yml
