# 开发规范 — 真机调试与图片配置

> 适用范围：微信原生小程序（`miniprogram/`）。所有图片相关功能、相册、选片、分享、二维码，必须以**真机扫码**运行结果作为唯一验收标准。

## 1. 真机调试流程（强制）

- 开发完成后，使用**微信开发者工具 → 预览**，生成调试二维码，用微信扫码进行**真机调试**。
- **禁止仅使用模拟器查看效果**：图片、相册等必须以扫码真机运行结果为准。
- 模拟器能看到 ≠ 真机扫码能看到，必须以扫码真机效果作为验收标准。

## 2. 图片资源规则（强制）

- 所有作品、相册、选片图片**全部使用公网 https 网络图片地址**。
- **禁止使用**：本地相对路径、`file://` 本地路径、`localhost` 地址（真机扫码无法加载本地图片）。
- 图片域名必须在微信小程序后台 **开发 → 开发设置 → 服务器域名** 配置：
  - `request 合法域名`：`https://yezhe-studio-server.onrender.com`
  - `downloadFile 合法域名`：`https://yezhe-studio-server.onrender.com`、**腾讯云 COS 图片 CDN 域名（即后端环境变量 `COS_CDN_DOMAIN`，如 `https://yezhe-xxx.cos.ap-guangzhou.myqcloud.com` 或自定义加速域名）**
  - `downloadFile 合法域名（R2 兜底，未启用 COS 时）`：`https://yezhe-img-proxy.yezhe128627.workers.dev`
  - 不配置 → 真机图片直接空白不显示。
- 开发者工具勾「不校验合法域名」仅本地调试绕过，真机/体验版仍受白名单约束。

## 3. 测试校验点（真机扫码逐项确认）

① 真机扫码打开小程序，进入客户相册页面，所有案例图片、订单相册图片正常渲染，不空白、不裂图；
② 打开作品案例详情，图片滑动浏览正常；
③ 进入选片页面，批量加载多张图片，真机不卡顿，图片可正常查看；
④ 分享相册，真机预览分享卡片可以正常读取相册第一张作为分享封面图；
⑤ 下载二维码功能，真机可以正常生成并保存图片到手机相册，toast 提示正常。

## 4. 问题兜底逻辑（强制）

- 图片加载失败**必须增加默认占位图**，显示灰色图片占位，**不要页面直接崩溃**。
- 任何 `<image>` 组件都应绑定 `binderror` / `bindload`，失败态走占位，不阻塞页面其余功能。

## 5. 关键提醒

- 模拟器图片能看到 ≠ 真机扫码能看到。
- 所有涉及图片的功能（相册、选片、作品、二维码下载、分享封面）在提交/发布前，必须用真机扫码走完第 3 节 5 个校验点。
