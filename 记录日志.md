# 开发日志 · 岛像工作室平台（摄影工作室管理系统）

> 项目路径：`/Volumes/勿删勿动/workbuddy/岛像工作室平台开发/摄影工作室管理系统/`
> 仓库：`https://github.com/YEZHE77/yezhe-studio`（main 分支，push 自动重建）
> 后台：`https://yezhe-studio-server.onrender.com` ｜ 网页后台：`https://yezhe-studio.pages.dev`（`admin/admin123`）
> 前端技术栈：React + Vite + Tailwind；后端：Node-Express（node:sqlite 开发 / Neon Postgres 生产）

---

## 一、已完成并提交的改动

### 1. 套系分享二维码弹窗（commit `65a0d60`，已 push）
- 文件：`client/src/pages/Packages.jsx`（新增 `PackageShareModal` 组件 + `openShareQr` / `copyShareLink`）
- 触发：点击套系列表卡片【分享】按钮 → 居中心浮层弹窗。
- 二维码：**后端动态生成、绑定套系 ID、不写死**。复用既有 `/api/shares`（`type=package`），查不到记录就 `POST` 新建一条，返回 `qr_url` / `share_url`。
- Tab：【H5】（默认蓝 `border-brand`）/【小程序】（灰）；小程序 Tab 用同一张二维码做圆形 + 微信绿环（`0 0 0 6px #fff, 0 0 0 7px #07C160`）+ 绿底小程序角标装饰。
- 文案「使用微信扫描以上二维码」+ 蓝色【复制链接】（点击复制并提示「链接已复制」）。
- 关闭：点蒙层 / 右上角叉号；内层 `stopPropagation`。
- 真机验证（puppeteer-core + Chrome）：分享点击→弹窗打开、两 Tab 切换、二维码 data URL 渲染（5098 字符）、蒙层点击关闭，全部通过。**未新建后端接口**。

---

## 二、代码完成、待构建/验证/提交的改动

### 2. 套系列表页改横向条目布局（文件 `client/src/pages/Packages.jsx`）
按新【套系列表页面】spec 重构（与早前纵向卡片版不同），保留全部原有 handler 与分享弹窗，未删旧代码：
- 顶部搜索占位「套系名称」；黑色「搜索」+「高级设置」按钮。
- 深色操作栏 `#2c2c2c` → **`#333333`**；分类下拉旁新增「更多筛选」列表图标按钮（切换高级设置面板）。
- 橙色空态条改为**常驻浅黄通知栏** `#fff8e1` + 橙色警示 `#ff9f43`：「您目前没有上线的促销/拼团活动…」+ 蓝色「去设置>」。
- 列表由纵向卡片改为**横向条目**（`divide-y` 分隔）：左封面 `w-20 h-20`、名称加粗、价格行「价格：¥x,xxx **|** 定金：¥x,xxx」价格数字红色 `#e4393c`、灰色菱形分类标签、右侧图标按钮组（分享｜编辑｜下架｜删除）。
- 新增内联 SVG 图标 + `IconBtn` 纯图标按钮组件。
- ⚠️ 与 spec 差异（待确认）：【编辑】/【+ 新建套系】仍复用原有**编辑弹窗**，未改为路由跳转 `nav('/packages/:id/edit')`。原因：「套系编辑页面」是独立的 4-Tab 详细 spec（用户早前发过，被插队），建议单独实现后再改跳转。

### 3. 订单详情页重构 + 图片管理真实上传（文件 `client/src/pages/OrderDetail.jsx` + 后端）
按【查看订单】spec 落地，保留全部原有 handler（收款/退款/作废/编辑/分享/选片/幻灯片）：
- **后端（无新建接口）**：`server/src/schema.js` 增量补列 `order_photos TEXT`（存 `{raw:[url], retouched:[url]}`）；`server/src/routes/orders.js` 的 `JSON_COLS` 增 `order_photos`，并在既有 `PUT /api/orders/:id` 接收落库（19 SET 列 + WHERE id 共 20 参数对齐）。
- **前端**：顶部浅绿色顶边横线 `#7ecdbb` + 订单编号/状态；**11 步流程进度条**（订单生成→生成合同→沟通确认→拍摄执行→拍摄结束→选片精修→预告片输出→全部精修完成→原片打包→统一交付→订单完结），当前步由 `status` 派生，「生成合同」需 logs 含'合同'、「原片打包」需 raw 非空才亮；**左侧垂直按钮组**（完成拍摄/分享订单/关闭订单/更多设置）；客户与订单基础信息卡 + 浅米绿服务详情条 `#eaf5ef`；**图片管理 Tab（原片/选片/精修片）真实上传**（原片/精修片走 `uploadBatch` 复用分片上传并落库，选片复用 `photo-select`）；**底部 Tab**（订单状态详情/交易记录/下载记录）。

### 4. 订单分享二维码弹窗（已存在，待重新真机验证）
- 既有实现（commit `61271cb`）：`server/src/miniQr.js` 生成环形小程序风格 SVG 二维码（相机图标 + 绿色小程序标识 `#07C160`）；`POST /api/orders/:id/mini-qr` 返回；`client/src/pages/Orders.jsx` 点击「分享订单」在按钮上方弹出，蒙层点击关闭、无叉号。
- 用户本轮重发该 spec，已确认实现与 spec 一致；待重启服务后跑 Chrome 真机验证收尾。

---

## 三、待处理 / 待确认

| 事项 | 状态 | 说明 |
|------|------|------|
| 套系编辑页面（4-Tab 详细 spec） | 未开始 | 用户早前发过独立 spec（套系名称/价格及问卷/服务及加片/其他详情），建议作为单独任务实现后，把列表页【编辑】/【新建】按钮改为路由跳转 |
| 订单详情页、套系列表页 构建+提交 | 待执行 | 本环境 Bash 工具此前不可用，未能 `npm run build` / 起服务 / Chrome 验证 / `git commit`；代码已人工复核 JSX 闭合与 SQL 参数对齐 |
| 订单分享二维码弹窗真机验证 | 待执行 | 重启 4000/5173 后跑 puppeteer 收尾 |
| 小程序 Tab 真实 `wxacode` | 暂缓 | 需 Render 配 `WX_APPID` + `WX_SECRET`（个人账号暂不配），届时再接 `wxacode.getUnlimited` |

---

## 四、本地验证 / 提交命令（待 Bash 恢复后执行）

```bash
cd "/Volumes/勿删勿动/workbuddy/岛像工作室平台开发/摄影工作室管理系统"

# 1) 后端（自动补列 order_photos）
cd server && node src/index.js &        # 默认 4000 端口
# 2) 前端
cd ../client && npx vite --port 5173 &  # 5173

# 3) 生产构建校验
cd ../client && npm run build

# 4) 浏览器核对
#    http://localhost:5173 → 工作台 → 套系（横向列表 / 分享弹窗）
#    → 订单中心 → 某订单「查看订单」（11 步进度条 / 图片管理真实上传）

# 5) 提交
git add client/src/pages/Packages.jsx client/src/pages/OrderDetail.jsx \
        server/src/schema.js server/src/routes/orders.js
git commit -m "feat: 套系列表横向条目 + 订单详情页 11 步进度条 + 图片管理真实上传"
git push origin main
```

---

## 五、提交历史（本阶段）

| commit | 内容 | 状态 |
|--------|------|------|
| `4c74234` | 套系页 1:1 重构为纵向卡片列表 | 已 push |
| `65a0d60` | 套系分享二维码弹窗 H5/小程序 Tab 动态生成 | 已 push |
| （待提交） | 套系列表横向条目 + 订单详情页 + 后端 order_photos | 待 build/commit |
